# 依赖与构建产物
node_modules/
package-lock.json
dist/
.astro/
.next/
__pycache__/
*.py[cod]

# ── 本地开发资产：仓库只发 skill 本体，研究/测试/样例不入库 ──────────────
# 这三个目录当前已不在仓库里。**规则保留是给以后用的** —— 若日后在本机重新生成
# 这类材料，它们会被自动排除，不会误提交。
#   docs/      立项调研与质量审计（研究过程资料）
#   testing/   测试用例集 + 判据负向测试 + 夹具（开发期基础设施）
#   examples/  实跑样例（产物示例，素材图片同样排除）
# skill 运行时**不读**这三个目录里的任何文件（SKILL.md 对它们零引用）。
# 注意：validate_skill.py 的 C2 会把指向这三个目录的引用降级为 info ——
#       否则这类引用会被当成断链刷警告，而那是预期状态不是缺陷。
docs/
testing/
examples/

# 示例里的素材是本机资产，不入库（见 examples/README.md）
examples/*/assets/

# skill 运行期在用户目录里生成的中间产物，误入仓库会污染
**/source-map.md
**/text.md
**/manifest.json
**/_WARNINGS.txt

# 用户提供的源材料，一律不入库
*.pdf
*.docx
*.pptx
*.xlsx

# OS / 编辑器
.DS_Store
Thumbs.db
desktop.ini
.vscode/
.idea/
*.swp
