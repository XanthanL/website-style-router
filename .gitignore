# 依赖与构建产物
node_modules/
package-lock.json
dist/
.astro/
.next/
__pycache__/
*.py[cod]

# ── 本地开发资产：仓库只发 skill 本体，研究/测试/样例不入库 ──────────────
# 这三项目前在本机照常存在并使用，只是不进 git：
#   docs/      立项调研与质量审计（研究过程资料）
#   testing/   测试用例集 + 判据负向测试 + 夹具（开发期基础设施）
#   examples/  三个实跑样例（产物示例，素材图片本来也已排除）
# skill 运行时**不读**这三个目录里的任何文件（SKILL.md 对它们零引用）。
# 注意：validate_skill.py 的 C2 会把指向这三个目录的引用降级为 info ——
#       否则干净克隆会刷出几十条断链警告，而那是预期状态不是缺陷。
docs/
testing/
examples/

# 示例里的素材是本机资产，不入库（见 examples/README.md）
examples/*/assets/

# skill 运行期在用户目录里生成的中间产物，误入仓库会污染
**/source-map.md
**/text.md
**/manifest.json
**/_WARNINGS.txt

# 用户提供的源材料，一律不入库
*.pdf
*.docx
*.pptx
*.xlsx

# OS / 编辑器
.DS_Store
Thumbs.db
desktop.ini
.vscode/
.idea/
*.swp
