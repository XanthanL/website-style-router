# Intake — Grill 决策树 + 约束筛选

> 借鉴 grill-me（julianoczkowski/designer-skills）的方法：**先把意图问清除，再选型**。

## 给 agent 的用法

- **一次只问一个问题**，不要一次性甩出问卷。
- 每个问题都给一个**推荐答案**（基于已有内容 / 文件 / 会话上下文推断）。
- 能从用户给的内容或文件推断出的，**先推断并请用户核对**，不要硬问。
- 用户确认后再进下一支；上下文已明显回答的分支直接跳过。
- 走完产出 `intent-summary.md`（意图摘要）。

---

## Grill 决策树（12 支，按顺序；可跳过已明确的）

### B0 · Source & Delivery（源材料与交付形态）★最先问

- Q0a **你手上的内容是什么形态？** 推荐答案：一份 PDF / 一个图片文件夹 / 一个现有网站的 URL / 一段话 / 什么都没有。
  → 决定走 `source.md` 的哪条提取路径。**没问清就开始做是最贵的错误。**
- Q0b **交付形态是哪种？** 推荐答案按上下文推断：
  - `standalone` 独立站（新域名/新项目）
  - `embedded` 嵌入已有站的某个路由（把内容挂到现有主站上）
  - `system-only` 只出设计系统与 token，不写页面
  → `embedded` 必须再读 `host-align.md`，且**立刻采集宿主令牌**（顶栏/底栏/色板/字体/语言）。
- Q0c **有没有想适配或模仿的网站？** 推荐答案：有 → 请给 URL 或截图（走 `source.md` 的「参考站采集」）；
  没有 → 从本 skill 的锚点库推导。
  ⚠️ 若答案是"就照我们自己另一个站那样"，那其实是 `embedded`，走宿主对齐而不是参考站采集。

### B1 · Audience & Access（受众与可达性）
- Q1 这站主要给谁看？推荐答案：路人/潜在客（公开）· 回头客（公开但重识别）· 专业评审/同行 · 内部/私域。
- Q2 有没有特定角色差异（如 admin / 普通访客 / 合作方）？推荐答案：无 → 单角色，无需权限层。

### B2 · Goal & Success（目标与成功定义）
- Q3 用一句话说清「做好了长什么样」。推荐答案：用户能 [某动作] / 能查到 [某信息] / 看完愿意 [转化]。
- Q4 核心转化目标是什么？推荐答案：`无` / `到店` / `联系` / `购买` / `报名` / `投递`。**答"无"是合法答案，且会大幅改变 IA 与布局。**
- Q5 最小可用版本（MVP）是什么？推荐答案：单页 + 唯一主动作，或单页 + 状态板。

### B3 · Content & Assets（内容与资产）★含图文策略
- Q6 手上实际有什么内容：文字量级 / 图片张数 / 视频 / 3D / 音频 / PDF。答不上写 `未知`，**不要脑补**。
- Q7 视觉资产质量：`专业摄影` / `手机随拍` / `扫描件` / `无`。**这条权重最高，直接决定图文策略。**
- Q8 **要不要展示图片？** 推荐答案（按 Q7 派生）：
  - 资产质量高 → `要，且是主角`（editorial-hero / catalog-grid / split-narrative）
  - 资产质量低或没有 → `不要图，靠排版/材质/色块撑`（utility-board / minimal-state），避免暴露低质图
  - 实体商品但暂时没图 → `要产品图占位，[待填] 显式标记`，绝不拿氛围图替代
- Q9 **要不要图标？** 推荐答案：
  - 需要表达状态/分类（在售/售完/品类）→ `functional 图标`（utility-board / data-dense）
  - 纯氛围 → `decorative 图标`（warm-hospitality / soft-organic）；严肃/极简 → `none`
- Q10 品牌资产现状：`完整 VI` / `只有 logo` / `只有名字` / `什么都没有`。
- Q11 **语言策略？** 推荐答案：宿主站是双语的 → `双语（复用宿主既有译文）`；独立站面向国内 → `纯中文`；
  面向国际/展览申请 → `中英双语`。**必须同时确认**：默认语言、切换器样式（是否照宿主站同款）、
  是否要防首屏语言闪烁。
  ⚠️ 双语不是"最后再加"的功能——它影响字阶（中英字宽不同）、按钮宽度、导航栏容纳量、灯箱文案，
  事后补会牵动全站。**在落定之前定。**

### B4 · IA Fit（信息架构适配）
- Q12 内容量级与更新频率：多少条目；`日更`/`月更`/`季更`/`一次性`。→ 隐含 IA（高频单源→directory；SKU 多→catalog；长文→narrative/portfolio）。
- Q13 谁维护：`本人（非技术）` / `本人（技术）` / `有专职` / `没人`。→ 非技术则必须单一数据源 + 纯静态。

### B5 · Interaction & Device（交互与设备）
- Q14 主要设备：手机 / 桌面 / 各半。→ 决定触控目标尺寸与栅格断点。
- Q15 使用频率：每日高频 / 偶尔。→ 决定信息密度与是否要状态可见。
- Q16 要不要通知/邮件/触发动作（如售完提醒）？推荐答案：暂不需要 → 纯静态。

### B6 · Constraints & Compliance（约束与合规）
- Q17 硬约束：营业时间、物理空间、法规、预算、交付期、不能用的东西。
- Q18 **是否实物商品 / 食品？** 命中 → 走下方「实物商品专项」，合规信息独立成区、不可折叠。

### B7 · Scope（范围）
- Q19 这是全量愿景还是 MVP？推荐答案：MVP 先跑通，v2 再扩。

### B8 · Visual Axis & Blocks（视觉收束）★别等用户来提

这三条是**用户最容易在交付后才想起来、且返工成本最高**的项。落定前一次性问掉：

- Q20 **中轴怎么定？** 推荐答案（按 IA 派生）：
  - 信息密集 / 台账 / 目录 → `left-rail`
  - 人物 / 作品集 / 宣言型 → `center-axis`（hero、肖像、封面居中；正文保持左对齐）
  - 长文交错 → `split`
  → 落成 `--axis` 写进 MASTER.md，见 `layouts.md`「中轴策略」。
- Q21 **区块去留？** 列出打算做的全部区块，逐块问"要 / 不要 / 合并"。
  ⚠️ 默认别做联系区（Contact / 邮箱 / 社交）—— 用户常常直接删掉。**没明确要就不做。**
- Q22 **尺寸与主次？** 主图 / 肖像 / 封面的三档目标宽度（如 320 / 440 / 620），一次定完，
  避免"再大一点""再居中一点"的多轮往返。

### B9 · Delivery Form（交付形态复核）
- Q23 回到 Q0b 复核：`standalone` / `embedded` / `system-only`。
  若为 `embedded`，确认已按 `host-align.md` 采到宿主令牌，并确认**宿主站已有实体的路径不受影响**。

### B10 · Tech Stack（技术栈输入）★Phase 1.5 的输入

技术栈的 8 个判定项（D1–D8）**大多能从 B0–B9 推断**，不要重复问。只有下面 4 项
推不出来、且错了返工最贵，必须在这一支里问掉：

- Q24 **谁来部署和维护？** 推荐答案：按 Q13 派生 ——
  - `本人（非技术）` → 排除一切需要 Node / 命令行的栈（只剩 `plain-html`）
  - `本人（技术）` / `有专职` → 可用构建型栈（`astro` / `vite` / `next`）
  → 对应 D3，是**排除力最强**的一条。
- Q25 **部署到哪里？** 推荐答案：静态托管（GitHub Pages / 对象存储 / Cloudflare Pages）
  / 已有服务器 / 还没有。
  → 静态托管排除 SSR；已有服务器且需鉴权 → 才考虑 `next` / `nuxt`（D7）。
- Q26 **有没有登录、表单提交、实时数据这类「要服务端」的需求？** 推荐答案：
  没有 → 纯静态；有 → 确认是第三方服务（表单可用 Formspree 等）还是必须自建后端。
  → 对应 D7；**答「第三方服务」仍可留在静态栈**，别一听到表单就上框架。
- Q27 **团队/你自己已经熟什么？** 推荐答案：不熟 → 按需求选，不为「技术新」而选；
  已熟 Vue → `nuxt`；已熟 React → `next`；已熟 Astro → `astro`。
  → 技术栈的**学习成本也是约束**，熟栈在同分候选里优先。

> 收齐后进 Phase 1.5：跑 `python scripts/pick_stack.py`（见 `techstack.md`），
> 产出 `tech-stack.md`。**技术栈不单独设人工确认点**，它与 IA / 锚点 / 中轴 / 字体一起在 G2 点头。

### B11 · Page Granularity & In-page Nav（页面粒度与页内导航）★v0.9

IA 的四选一只回答了「**这一页由哪些区块组成**」，没回答「**内容该分成几页**」。
这一支问的是后半句 —— 大部分输入能从 B3 / B4 推断，**只有 3 项必须问掉**：

- Q28 **条目会不会被单独分享？** 推荐答案（按 B4 内容量级派生）：
  - 条目 ≥4 且每件有独立深度内容（多图 / 多段）→ `会`（作品 / 商品 / 案例 / 文章都是这类）
  - 条目 ≤3 或每条只有一行属性（豆单 / 价目表 / 营业信息）→ `不会`
  → 决定 `single` 还是 `master-detail`（对应判据 `shareable`）
- Q29 **需不需要单条被搜索引擎单独收录？** 推荐答案：
  - 商品 / 作品 / 案例 / 文章 → `要`（这是流量入口）
  - 「今天有什么」/ 单次活动页 → `不要`
  → 对应判据 `indexable`
- Q30 **除主体内容外，还有几个「内容量足以独占一页」的并列主题？**
  （关于 / 服务 / 案例 / 博客 / 招聘 / 多语言版）
  推荐答案：数一遍，**只有一段话的不算**。≥2 → `multi-page`。→ 对应判据 `themes`

> ⚠️ **不要问「你要单页还是多页」** —— 用户答不上来，且答案会是「都行」。
> 用上面三问**推断**，把结论当推荐答案给出去，让用户一秒否决（`decide.md` 的 B 类格式）。

收齐后进 Phase 1.7：跑 `python scripts/pick_pages.py`（见 `architectures/granularity.md`），
把两轴结论写进 `content-profile.md`。**与 IA / 锚点 / 中轴 / 字体一起在 G2 点头。**

> **页内导航不需要单独问。** 它由「页面区块数 + 有没有查阅型区块 + 是不是线性阅读」算出来，
> 这三项在 B3 / B4 / B6 已经拿到了。**能算的不要问。**

---

## 约束筛选（输出"不能做什么"）

逐条判定，把排除项写进 `content-profile.md`。**输出是排除清单，不是选择。** 新增「布局」列。

| 约束维度 | 判定 | 排除 IA | 排除布局 | 排除风格 |
|---|---|---|---|---|
| 视觉资产质量 | 低 / 无 | — | editorial-hero、catalog-grid、split-narrative（依赖图） | editorial-print（低质配图毁掉）、warm-hospitality（变廉价滤镜）、expressive-artistic |
| 内容密度 | 低（<10 条目） | bento / 多级导航 | catalog-grid | — |
| 内容密度 | 高（>50 条目） | 极简单页、纯叙事长滚 | minimal-state | — |
| 更新频率 | 日更 | 硬编码内容进组件的 IA | — | — |
| 维护者 | 非技术 | 需命令行/CMS 的 IA | — | — |
| 转化目标 | 无 | conversion | editorial-hero（空） | — |
| 受众耐心 | 低 | narrative | split-narrative（长） | — |
| 卖不卖空间体验 | 否 | — | — | warm-hospitality |
| 明暗 | 只在白天 | — | — | quiet-luxury（破坏呼吸感）、glass-tech（深色底） |
| 多语言 | 是 | — | — | 依赖等宽标题/文字做图形的风格 |
| 是否实物商品/食品 | 是 | — | — | 见专项 never；合规区不可折叠 |
| **交付形态** | embedded（嵌入宿主站） | — | 宿主已有的布局原型（**服从宿主，不另起一套**） | 宿主已有的视觉语言（色板/字体/圆角/动效一律服从宿主） |
| **交付形态** | system-only | 全部 IA | 全部布局 | — （只出 token） |
| **参考站** | 用户指定了模仿对象 | — | 与参考站结构冲突的布局 | 与该站同类的风格（避免做成它的劣质复制品） |
| **语言** | 双语 | — | 依赖等宽对齐表头的布局（中英字宽不同会错行） | 文字做图形/等宽标题类 |
| **维护者技术程度** | 非技术 | 需命令行/CMS 的 IA | — | — （且技术栈只剩 `plain-html`，见 techstack.md） |
| **渲染需求** | 需要 SSR | — | — | — （技术栈只剩 `next` / `nuxt`） |
| **中轴** | center-axis | — | — | 要求"全左对齐"的信息型风格 |
| **素材体积** | > 15MB | — | — | — （须先压缩/响应式化，见 source.md） |

> **这张表排的是「不能做什么」（IA / 布局 / 风格）。页面粒度不在这里排** ——
> 它不是「排除」出来的，是**算**出来的（`entries` / `depth` / `shareable` / `indexable` / `themes`）。
> 见 `architectures/granularity.md` 与 `scripts/pick_pages.py`。

### 实物商品 / 食品专项（命中即底线，非可选项）

**必须有（且不可折叠、字号 ≥12px）**：品名、配料表、净含量、保质期、贮存条件、生产者、生产许可证编号（SC）、过敏原、食用方法、发货/冷链说明。

**必须没有（法律红线，与风格无关）**：疾病预防/治疗功能宣称（"健胃""抗菌""预防流感"）、极限词（"最""第一""国家级""顶级"）、编造评价/销量/排名、未经核实的功效数据。

⚠️ 品类百科（如百度百科）常写满食疗功效，直接抄到商品页即违规。百科能写，商品页不能写。

**数据缺失**：用 `[待填]` 占位并在页面上**显式标记**（红色虚线框），绝不编一个看起来合理的数。占位内容悄悄上线是这类项目最贵的一类事故。

---

## 输出

`intent-summary.md`（Grill 产物）+ `content-profile.md` 必须含四段：

```
## 意图摘要（intent-summary）
- 源材料：[类型 + 路径] → 提取方式 + 溯源表位置（见 source.md）
- 交付形态：[standalone / embedded / system-only]（embedded 时附宿主令牌来源文件）
- 成功定义：[一句话]
- 转化目标：[无/到店/购买/…]
- 图文策略：图片=[要/不要/占位]；图标=[functional/decorative/none]
- 语言策略：语言=[纯中文/中英/多语]；默认=[zh/en]；切换器=[照宿主同款/自建]
- 参考站：[URL 或 无]（有 → 学什么 3 条 / 不学什么 2 条，见 references.md）
- **技术栈：[选定的栈]（D1–D8 判定 + 被排除项见 tech-stack.md）**
- **页面粒度：[single / master-detail / multi-page]（页面清单 + 依据 + 被排除档位见 content-profile.md）**
- **页内导航：[none / anchor-jump / sticky-toc / section-rail]**
- 中轴：[left-rail / center-axis / split]
- 区块与尺寸：区块=[逐块要/不要]；主图/肖像/封面目标宽度=[x/y/z]
- 硬约束：[列出]

## 画像
（B 系列答案原文，不加工）

## 排除
- 排除 [X] 型 IA，因为 [第 N 条]
- 排除 [Y] 布局，因为 [第 N 条]
- 排除 [Z] 风格，因为 [第 N 条]

## 剩余空间
- 技术栈：只剩 [S]，因为 [D1/D3/D7/D8 中哪几条] 排除了其余（详见 tech-stack.md）
- IA：只剩 [X]，因为其余被 [原因] 排除
- 布局：只剩 [L]，因为 [风格 Z 绑定 L] 且 [约束]
- 风格：剩余约束为 [列出]，满足 [A/B/C]，取 [A]，因为 [一条具体理由]

## 待补（指不回源的内容）
| 字段 | 现状 | 需谁提供 | 页面上如何标记 |
|---|---|---|---|
| contact.email | 无 | 用户 | `[待填]` + 红色虚线框 |
```

**最后一句"取 A 因为…"必须是可证伪的具体理由**，不能是"更合适""更贴合"。写不出就说明约束不够，回 B 系列补。
