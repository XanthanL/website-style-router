# Decide — 决策权归属（谁决定什么）

> 本 skill 最常见的失败不是"选错风格"，而是**该问的没问、不该问的反复问**。
> 两个方向的错都要防：问太多 → 用户觉得在填问卷；问太少 → 用户事后逐个指导（本次真实情况）。

## 判据（只此两条，相乘）

```
① 这个决定错了，返工成本有多高？（改一行 CSS / 重做全站）
② 我有没有可靠依据？（源材料里写着 / 我能推断 / 我完全不知道）
```

| 成本 | 依据 | 档位 |
|---|---|---|
| 低且可逆 | 有 | **A · 自主** |
| 高或不可逆 | 有依据能推 | **B · 推断 + 一次确认** |
| 高或不可逆 | 无依据 | **C · 必须问** |
| 涉及法律 / 真实性 / 破坏性 | — | **D · 绝不代做** |

---

## A · 自主决策（不问，交付时在说明里列出）

**原则**：改起来便宜、做错了也无所谓的东西，不要拿去占用用户的注意力。

- 文件与目录结构、命名规范、代码风格、组件拆分粒度
- CSS 变量命名、单位选择、是否用 Tailwind（除非用户指定）
- 栅格断点具体数值、间距档位的具体 px
- 动效时长与缓动曲线（在 token 规范内）
- 语义化标签、`alt` 文案（按内容生成）、`aria` 属性
- 图片压缩参数、格式选择（webp/jpg）、懒加载策略
- 组件内部实现（灯箱的键盘绑定方式、状态管理方式）
- 字体**加载**细节（`font-display` 策略、preload、子集化、回退栈顺序）—— 注意：字体**搭配**（选哪套字体）属 B 类，不在此列

**唯一要求**：交付说明里**列出来**（"以下是我自主决定的"），让用户有机会否决。

## B · 推断 + 一次确认（给推荐答案，用户一句话即可通过）

**原则**：能推出来的就不要硬问，但要**明说推的依据**，让用户能一秒否决。

格式固定：

```
推荐：<答案>，因为 <依据>。若不符请指出。
```

一屏**最多 4 个**（它们彼此独立，答案是并列关系）。用户回"就按你说的"即全部通过。

B 类清单：

| 决定 | 推荐依据来自 |
|---|---|
| **技术栈（7 选 1）** | Grill B10 + `techstack.md` 的 D1–D8：**先排除、再定序**（`scripts/pick_stack.py`） |
| IA 骨架（portfolio/narrative/directory/conversion） | Grill B2 转化目标 + B4 内容量级 |
| **页面粒度（single / master-detail / multi-page）** | Grill B11 三问 + `architectures/granularity.md` 的判据（`scripts/pick_pages.py`）。**不问「你要单页还是多页」** |
| **页内导航（none / anchor-jump / sticky-toc / section-rail）** | 区块数 + 查阅型区块数 + 是否线性阅读 —— **纯计算，不占用提问预算** |
| 布局原型（5 选 1） | 风格锚点绑定 + 约束排除 |
| 风格族 → 锚点 | `styles/families.md` 的两级收敛 |
| 中轴策略（`left-rail`/`center-axis`/`split`） | 布局原型 |
| 图文策略（要图/不要图/占位 + 图标档位） | Grill B3 的资产质量 |
| 色板落地数值 | 宿主令牌（`host-align.md`）或锚点默认 |
| **字体搭配（display × body × mono × cjk）** | `styles/fonts.json` 的锚点条目（40 锚点 display 全库唯一） |
| 视觉签名（≥3 条结构手法） | `layouts.md` 的签名菜单 + 锚点 `never` 排除 |
| 区块清单（含默认不做 Contact） | IA + 转化目标 |
| 三档图片宽度 | 中轴 + 布局 |

## C · 必须问（不可推断，问不到就阻塞；问不到时用 `[待定]` 标记并继续，但必须在交付说明里列出）

**判据**：这个信息只有用户有，且我猜错会毁掉整个方向或违法。

| 必问项 | 为什么不能推 | 问法 |
|---|---|---|
| **源材料在哪 + 什么类型** | 猜错则全部返工 | "内容是以什么形式给我的？" |
| **交付形态**（独立站 / 嵌入已有站 / 只出 token） | 决定后半程全部工作 | "这是新站，还是挂到现有某站的一个页面？" |
| **核心转化目标**（无 / 到店 / 购买 / 联系 / 报名） | 决定 IA，猜错整站重做 | 给 6 选 1（含"无"） |
| **参考站 / 模仿对象 URL** | 用户心里有具体对象，我无从得知 | "有没有想贴近的网站？给个链接或截图" |
| **语言策略**（单语 / 双语 / 多语 + 默认语言） | 影响字阶、按钮宽、导航容纳量，事后补牵动全站 | 给 3 选 1 |
| **实体信息**（真实店名、地址、电话、SC 编号、价格、营业时间） | **编造 = 事故** | 逐项列出来，允许 `[待填]` |
| **法律主体与资质** | 涉及合规 | "是个体户/公司/个人？" |
| **合规红线** | 品类相关的禁用表述 | "有没有行业禁语或不能提的内容？" |
| **维护者是谁、技术程度** | 决定数据源形态 | 4 选 1 |
| **预算与交付期** | 决定 MVP 范围 | 可给推荐 |
| **已有品牌资产**（VI / logo / 色值 / 字体授权） | 授权问题无法推断 | 列出可选项 |
| **必须保留 / 必须去掉的区块** | 用户偏好 | 逐块列 |

**⚠️ 反面清单 —— 这些不要问：**

- 能自己查的（框架文档、依赖用法、CSS 属性）→ 查
- 用户已经在源材料或上文说过的 → 复述确认，不要重问
- 可以事后零成本改的（间距、字号）→ 归 A 类
- 开放式审美问题（"你想要什么风格？"）→ **禁止**。用户答不上来，且答案是均值。改为问三个决定项（见 families.md 三问表）

## D · 绝不代做（红线，即使用户要求也要先警示）

- 编造数据：假地址、假电话、假评价、假销量、假排名、假年份
- 编造资质：假 SC 编号、假认证、假奖项
- 违规宣称：疾病预防/治疗功能、极限词
- 摘抄第三方受版权保护的长文/图片而不标注来源
- 破坏性操作：删除用户文件、`rm -rf`、覆盖未备份的现有站点
- 替用户做法律/财务决策

## 提问预算与节奏

```
总轮次      ≤ 3 轮（超过说明源材料不足，回头补源，不是继续问）
C 类问题    可一屏多问（≤4 个），因为它们彼此独立
B 类问题    必须一次一问（答案会改变后续问题）
推荐答案    每个问题都必须给，写在选项的第一位并标注（推荐）
用户答"随便"  → 视为采纳推荐答案，继续
用户不答某项  → 标 `[待定]` 继续；交付说明里单列，不得静默填值
```

**超出预算怎么办**：不是继续问，而是回到 Phase -1 —— 说明源材料不够，请用户补内容。**问得越多不代表越专业。**

## 必须主动停下来的 5 个触发条件

以下任一成立，无论进度如何都要停下来等人点头：

1. Phase 2 落定（技术栈 + IA + **页面粒度 + 页内导航** + 风格 + 布局 + 中轴 + 字体 + 语言）之后 —— 这是唯一的强制确认点
2. 需要用户提供 C 类信息才能继续（源、形态、转化目标、语言）
3. 发现源材料自相矛盾（同一人两个年份 / 两个名字）
4. 发现合规风险（品类涉及食品/医疗/金融/教育等）
5. 操作可能影响宿主站已有实体（改公共组件、改路由）

## 交付时必须主动交代的三件事

1. **我自主决定了什么**（A 类清单）
2. **哪些是推断的、依据是什么**（B 类清单）
3. **哪些还没确定**（`[待定]` / `[待填]` 清单，以及它们在页面上的可见标记位置）

> 这一节是本次教训的直接产物：上一轮交付时没交代这三件事，用户只能在看到成品后逐个发现
> （"可以居中吗""Contact 可以先删掉吗""是不是没有双语"）。**说清楚边界，比做得多更重要。**
