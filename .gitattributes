# 统一用 LF 换行。
# 不加这份文件，Windows（core.autocrlf=true）与 macOS/Linux 的开发者
# 会在每次 add 时互相翻转行尾，diff 里出现整文件改动，白白消耗 review 精力。
# 提交进仓库的一律 LF，工作区也保持 LF。
* text=auto eol=lf

# 二进制不参与换行转换与 diff
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.pdf binary
