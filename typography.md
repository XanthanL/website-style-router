# Typography — 排版数值系统

> 页面的「好看」约 70% 由排版数值决定，配色只占后半段。这份文件把审美判断
> 变成可取值、可复核的数字。**改字号/行高/行长前先读这里**，否则改的一定是印象。

数值来自 `styles/signatures.json` 的锚点签名，展开逻辑在 `scripts/emit_tokens.py`。
本文说明**为什么是这个带**，以及**什么时候可以出带**。

---

## 0 · 字体搭配（先选字，再算数值）

**字体的辨识度高于配色与字号。** 同一份布局，把标题从 Inter 换成 Fraunces，观感变化
远大于换掉强调色。所以字体是**选型的一等公民**，不是 token 生成时的兜底常量。

> ⚠️ 这一节是 v0.7 新增的。旧版把字体塌缩成 `serif / sans / mono` 三个通用栈，
> 于是 40 个锚点产出的 `--font-display` 几乎都是 `"Inter", -apple-system…` ——
> 数值全过、观感全同，就是「千篇一律」最直接的技术成因。

### 搭配从哪来

**不要现场决定字体。** 每个锚点的搭配在 `styles/fonts.json` 里已经定好，由
`emit_tokens.py` 落成四个变量：

```
--font-display  ← 标题（具名字体，40 个锚点全库唯一）
--font-body     ← 正文（与 display 不同字体）
--font-mono     ← 数字 / 规格 / 代码（tabular-nums 靠它）
--font-cjk      ← 中文回退（不让中文字体渲染西文）
```

### 五种搭配范式

| 范式 | 结构 | 适用 | 例子（锚点 → display × body） |
|---|---|---|---|
| **衬线 × 无衬线** | 衬线标题压场，无衬线正文保可读 | 编辑 / 机构 / 静奢 / 怀旧 | `editorial-hero` → Playfair Display × Lora |
| **展示体 × 无衬线** | 超重 / 压缩 / 变形标题当图形 | 商业标签 / 宣言 / 实验 | `provisions-label` → Anton × Inter |
| **无衬线 × 无衬线** | 两个不同家族的 grotesk | 网格 / 科技 / 企业 | `swiss-utility` → Archivo × Inter |
| **等宽 × 无衬线** | 等宽只做标题或只做数字 | 终端 / 档案 / 工程 | `archival-index` → Martian Mono × Inter |
| **中文体 × 西文体** | 汉字承担图形，西文承担数字 | 东方 / 地域 / 书法 | `ink-wash` → 马善政（标题）× 思源宋（正文） |

### 六条硬规则（`fonts.json` 的 `_rules`）

1. **具名**：`--font-display` 首族必须是具名字体，通用栈只能出现在回退位置。
2. **分离**：display ≠ body。同一字体当标题与正文 = 没做搭配。
3. **全库唯一**：40 个锚点的 display 字体不重复（机器校验：`validate_skill.py` F8）。
4. **中文回退显式**：显式声明 CJK 栈，且不让中文字体渲染西文。
5. **字重克制**：实际使用的字重 ≤2，加载时别拉全家桶。
6. **可获取**：只用开源 / 免费可商用字体。

### 加载策略（由 Phase 1.5 的技术栈决定）

`fonts.json` 的 `loading` 字段：`self` 自托管（默认，隐私好、无第三方请求）/
`cdn` CDN 引入（需大量字重，或 CJK 字体过大不宜自托管）/ `system` 系统回退。

**中文一律优先 `system`**：一套完整中文字库动辄 3–8MB，自托管会拖垮首屏。
只有笔画特征本身是风格主张时（`ink-wash` 的马善政、`seal-script-cn` 的站酷小薇）
才走 `cdn`，并且只加载用到的字数。

自托管时配 `font-display: swap`，并把 `woff2` 放 `assets/fonts/`；
不要 `@import` 到 CSS 顶部（阻塞渲染），用 `<link rel="preload" as="font" crossorigin>`。

### 字体选完，再回到下面的字阶

字体的**字形特征**会影响数值取值：高对比衬线的 display 需要更大字号才撑得住，
等宽体的字宽固定，`ch` 与汉字的换算关系与比例字体不同。**先定字体，再算 §1–§7。**

---

## 1 · 字阶（Modular Scale）

一切字号由两个数生成：**公比 `r`** 和 **正文基准 `base`**。

```
size(n) = base × r^n      n ∈ {-1, 0, 1, 2, 3}      正文 6 级封顶
display  = base × r^4 或 r^5                        取决于 r
```

`r ≥ 1.4` 时用 `r^4`，否则用 `r^5` —— 大字少的站需要更大的跳跃才能拉开层级。

### 公比决定气质

| 族 | `r` 带 | 读起来的感觉 | 为什么 |
|---|---|---|---|
| grid · institutional · techno | 1.167 – 1.25 | 平、稳、像表单 | 信息密度高，层级靠字重和线，不靠大小 |
| editorial · craft · industrial · commerce · nostalgic | 1.25 – 1.333 | 正常书刊感 | 经典书籍字阶，1.25 是印刷业默认 |
| contemplative · vernacular | 1.333 – 1.5 | 疏、慢、有呼吸 | 大字与小字差得远，中间层被抽掉 |
| expressive | 1.5 – 1.6 | 冲突、宣言 | 标题要压过正文一个数量级 |

**带外即失真**：`r < 1.15` 层级塌陷（用户分不出标题和正文）；`r > 1.7` 正文小字会碎（`<12px` 中文不可读）。

### 三个常见错误

1. **手挑字号**（12 / 14 / 16 / 18 / 24）→ 级差在 1.17、1.14、1.125、1.33 之间乱跳，视觉上"不整齐"但说不出哪不对。**等比才有秩序感。**
2. **只改大标题**：`r` 一改，6 级全变。改标题不动 `r`，就是把字阶撑歪。
3. **移动端等比缩 `base`**：`base` 减 1px 即可，`display` 要单独限幅（手机上头图标题 ≤ 34px，否则一行放不下 4 个汉字）。

---

## 2 · 行高（Leading）

**行高不是常数，是字号的函数。** 字号越大，行高倍数越小——因为大字本身的行间空隙已在视觉上足够。

```
lh(display) = clamp(1.02, lh - 0.62, 1.22)
lh(2xl)     = clamp(1.08, lh - 0.48, 1.32)
lh(xl)      = clamp(1.14, lh - 0.34, 1.42)
lh(lg)      = clamp(1.22, lh - 0.22, 1.52)
lh(base)    = lh                        ← 签名里的值
lh(xs)      = clamp(lh, lh + 0.05, 1.95)
```

### `lh` 取值（族带）

| 主体文字 | `lh` 带 | 说明 |
|---|---|---|
| 拉丁为主 · 短段落 | 1.45 – 1.6 | 西文 x-height 小，1.5 已够 |
| 中英混排 | 1.6 – 1.75 | 汉字方框充满字身，需更多行间隙 |
| **中文为主** | **1.7 – 1.95** | 汉字重心居中，1.7 以下会「挤」 |
| 单栏长文 | 1.8 – 1.9 | 行长越长，行高要越松（防跳行） |

> **反直觉但对**：行长越宽，行高要越大。窄栏可以紧，宽栏必须松。很多人反着做。

**行高必须无单位**（`1.7` 而非 `27px`），否则子元素继承的是固定像素，字号一变就崩。

---

## 3 · 字距（Tracking）

| 场景 | 值 | 理由 |
|---|---|---|
| 大字标题（`display`/`2xl`） | `-0.01` ~ `-0.04em` | 大字号的字间视觉空隙被放大，需收紧 |
| 中标题（`lg`/`xl`） | `-0.005` ~ `-0.02em` | 同上，程度减半 |
| 正文 | `0` | — |
| 全大写 / 微标签 | `+0.06` ~ `+0.12em` | 大写字母无升部降部，字间显挤，需撑开 |
| 中文正文 | `0`（**绝不加正字距**） | 汉字是满方框，加字距会让词散架 |
| 中文标题 | `0` ~ `-0.02em` | 仅在 `≥32px` 时轻微收紧 |

**字距随字号线性加深**：`trk(size) ≈ trk_display × (size / size_display)^0.5`。不必精算，记住「越大越紧、越小越松、大写要撑」即可。

⚠️ 中文**不要**套西文的 `letter-spacing: 0.05em` 之类的"高级感"配方——那是拉丁小写的解法，用在汉字上等于把每个字拆开。

---

## 4 · 行长（Measure）★最容易搞错

**`ch` 是半角单位**（字符 `0` 的宽度），不是「一个字符」。

```
中文行宽（字） ≈ ch ÷ 2        字体栈以 Latin 打头时 ≈ ch ÷ 1.8
```

| 族 | `ch` 带 | 折合汉字/行 |
|---|---|---|
| expressive | 44 – 52 | 22 – 26 |
| vernacular · contemplative | 50 – 58 | 25 – 29 |
| editorial · craft · commerce · nostalgic | 56 – 66 | 28 – 33 |
| grid · industrial · institutional | 62 – 78 | 31 – 39 |
| techno（等宽） | 68 – 80 | 34 – 40 |

通用区间：**45–75ch**。低于 45ch 换行太频（眼睛跳），高于 80ch 回行找不到下一行开头。

**做法**：正文列宽用 `max-width: <ch>ch`（或 `em`），**不要在正文上写 px**。容器宽 `--measure` 是另一回事——容器可以 1080px，正文列仍限 68ch。

---

## 5 · 中西混排

四件事，全是数值问题：

1. **字号配比**：同 `font-size` 时汉字视觉比拉丁大 **8–12%**。要让两者视觉等大，中文取 `0.94–0.97 ×` 西文字号；或标题中文比西文小一档。**正文一般同字号**，只在标题上做配比。
2. **间距**：中英/中数之间插 `0.25em` 空隙。手工写或靠 `text-spacing` 类特性；宁可空隙偏大不可偏小，粘连最难看。
3. **标点**：
   - 中文标点全角，占一个字身。**行首禁则**：`，。、；：？！）》」』` 不得出现在行首，`（《「『` 不得出现在行尾。CSS 用 `line-break: strict`（中日韩）。
   - **数字与西文用西文字体**（`--font-mono` 或 `--font-body` 的西文栈），别让中文字体渲染西文——字形和字宽都差。
   - `hanging-punctuation: first allow-end` 让引号/逗号悬挂到版心外，做渐进增强（Safari 支持较好，其余浏览器忽略即可，不能作为唯一手段）。
4. **禁用**：不要给中文正文设 `text-align: justify`（汉字间会被强行拉成沟）；不要 `font-style: italic`（汉字斜体是机械歪斜，极难看；要用衬线就换字体）。

---

## 6 · 段落与纵向节奏

| 项 | 值 | 说明 |
|---|---|---|
| 段间距 | `0.75 × lh`（约 1 个行高） | 用 `margin-block` 控制，不用空行 |
| 首行缩进 | **不用**（网页） | 中文 2em 缩进会破坏左轴；印刷才用 |
| 标题上间距 : 下间距 | **≥ 1.5 : 1** | 标题要「粘住」它下面的正文 |
| 大标题上方留白 | `≥ 1.5 × 标题行高` | 否则标题像被上面的内容压住 |
| 区块间距 | 栅格大值（`--space-6/7`） | 区块呼吸靠间距，不靠分割线 |

**所有纵向间距必须落在同一根栅格上**（`--space-*` 的整数倍）。混进 `18px`、`26px` 这种栅格外数值，页面会「说不出的不整齐」。

---

## 7 · 光学修正（专家与非专家的差距）

这些是「数值都对但看起来还差一口气」的解药：

1. **视觉边距**：大字标题以 `I` / `H` / `A` / `V` 起首时，左侧有光学空白，需负边距外扩 `0.02–0.05em`，才能与下方正文对齐。
2. **负字距**：大字标题不做负字距，字间会像被撑开（见 §3）。
3. **大写标题**：全大写必须加字距 + 略减字号，否则又挤又高。
4. **下划线**：`text-underline-offset: 0.12em` + `text-decoration-thickness: from-font`，避免压到降部（`g`/`y`/`p`）。
5. **数字对齐**：表格、价格、规格列必须 `font-variant-numeric: tabular-nums`，否则每行宽度不一。
6. **居中不只是 `text-align`**：图与图注必须同轴；`center-axis` 站上正文仍左对齐。见 `layouts.md`。

---

## 8 · 交付前的排版自检

跑一遍机器审计：

```
python scripts/audit_tokens.py <MASTER.md 或 tokens.css>
```

人眼再过四条（脚本查不了的）：

- [ ] 把页面缩到 375px，**标题没有一行只剩 1–2 个汉字**（孤字）——有就降一档字号或改断行
- [ ] 最长的那段正文，读到行尾回行时**不跳行**
- [ ] 中英混排处**没有粘连**（英文单词贴着汉字）
- [ ] 关掉所有动效后，**阅读节奏依然成立**（靠留白而非动画）

字体专项（§0，脚本查的）：`--font-display` 首族是否具名、display ≠ body、
display 是否与 `fonts.json` 一致。**F6 不过 = 字体没做搭配**，等于把风格区分让给了配色。

判据的完整清单与分级见 `design-qa.md`。
