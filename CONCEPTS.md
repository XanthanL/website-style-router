# 概念介绍 —— 这套 skill 是怎么想的

> **关于路径**：文中出现的 `examples/`、`testing/`、`docs/` 是**本机开发资产**，
> 不随仓库发布（见 `.gitignore`）。克隆下来的仓库里没有这些目录 —— 这不影响 skill 运行，
> `SKILL.md` 对它们零引用；只是文中对应的示例与自测命令在本机才有。

> 本文回答**「为什么」**：为什么它是这样分层的、每个名词到底指什么、改哪里会动到什么。
> 想知道**「怎么做」**（装在哪、跑什么命令、怎么加东西），看 [`TUTORIAL.md`](TUTORIAL.md)。
>
> **建议读法**：先读 §1–§4，建立整体框架；§5 是这套设计的核心洞察，不能跳；
> §6–§7 是给要动手改的人看的；§9 是一张速查表，随时回来查。

---

## 0. 一句话

**它把「网页长什么样」这个判断，从 agent 的即兴发挥，搬进三个可以被检查的地方：数据表、计算脚本、判据。**

市面上几乎所有同类工具只做「给你一堆风格，挑一个」。这套东西不解决「选什么」，它解决**「按什么顺序选、选完谁负责验」**。

---

## 1. 它要解决的五个真实失败模式

这套 skill 的每一条设计，都对应一个**真实发生过**的失败。理解失败比理解规则更重要——规则会变，失败模式不会。

前两个是 v0.7 修的，后三个是 v0.8 / v0.9 陆续补的。**它们共享同一个根因形态：某个维度不存在，或某条规则没有判据。**

### 失败一：产出「千篇一律、只是不同的 Markdown 组织器」

**症状**：不同行业、不同受众的站，做出来观感高度雷同；字体几乎都是 `Inter`；底色全是 `#FFFFFF`。

**根因**（三条，都可指到行）：

1. `scripts/emit_tokens.py` 把底色明度**写死**成 `oklch(0.985, …)` —— 不管选哪个锚点，`--bg` 都出近白。
2. `styles/signatures.json` 的结构是「一个锚点 = 一套常量」，不是参数空间 —— 同一锚点第二次使用必然产出**逐值相同**的 token。
3. `styles/fonts.json` 每个锚点只有**一对**字体，而规则又要求「display 全库唯一」—— 同一锚点复用即「无字可用」。

**对策**：见 §5 的三道防线。

### 失败二：技术栈被默认继承

**症状**：不管需求是「修鞋摊想让邻居知道还开着」还是「基金会年报要中英双语 + 图表」，产物都是同一套 `Astro` 项目骨架 + `src/pages/`。

**根因**：技术栈从未被**判断**过，它只是被隐式假设，写在「实现说明」表里。

**对策**：新增 **Phase 1.5 技术栈选型**（[`techstack.md`](techstack.md) + `scripts/pick_stack.py`）。需求 8 判定项 → **先排除、再定序** → 产出 `tech-stack.md`，必须写明被排除项和 3 条可观测证伪条件。**技术栈必须是被判断出来的，不是默认继承的。**

### 失败三：单站都合格，一批摆一起就塌

**症状**：每个站单独跑审计都是 `F 级 0`（完全合格），但五个站并排一看，是同一个东西。实测数据：

| 指标 | 实测（7 个站） |
|---|---|
| `--bg` 唯一率 | **2/7**（`#FFFFFF` 出现 6 次） |
| `display` 字体唯一率 | **5/7**（`Inter` 出现 2 次） |
| 中轴种类 | 2 种（只有 `left-rail` 和 `center-axis`） |
| 40 个锚点的使用率 | 3 个（**7.5%**） |
| 视觉签名 | 5/5 全部缺失 |

**根因**：**所有判据都是「单站内自洽」的，没有一条是「跨站差异」的。** 而排除法在批量场景下会**同向收敛**。

**对策**：见 §5。

### 失败四：所有站都退化成单个 `index` 首页（v0.9.0 修）

**症状**：内容一多就只能一翻到底；每个站都只有一个 `index`；详情页、目录、锚点跳转全都没有。设计达标了，**浏览功能性**没有。

**根因**：IA 只回答了「**这一页由哪些区块组成**」（portfolio / narrative / directory / conversion 四选一），
从未回答「**这些内容该分成几页**」，也没规定「**长页里怎么跳到关键部位**」。
**页面粒度这个维度在 skill 里根本不存在**，所以生成器无从产出多页。

**对策**：补两个**内容驱动**的维度——页面粒度（`single` / `master-detail` / `multi-page` / `n-a`）
与页内导航（`none` / `anchor-jump` / `sticky-toc` / `section-rail`）。见 §4.1b。

### 失败五：铁律写了，但没人检查（v0.9.1 修）

**症状**：34 条铁律里只有 14 条（41%）有机器判据。最有代表性的是铁律 19「字段定义了就必须渲染」——
**它自己的正文里就写着「引用校验还会 PASS」**，等于承认了漏洞却一直没补。

**根因**：同一个失效方式 v0.7 已经发生过一次（视觉签名）。这说明它不是偶然，而是**机制性缺口**：
写铁律比写判据容易得多，于是铁律会持续领先于判据。

这是一个**必须定期反审**的缺口：

> **用 skill 自己的信条反过来审它自己。** 不看铁律写了什么，而是问
> 「违反它的最小产物长什么样，有哪个脚本会报错」—— **答不出脚本名的就是欠账。**

**对策**：v0.9.1 一次性补了 5 条（铁律 14/18/19/26/29 → F16–F20）。见 §4.6 与 §7.1。

---

## 2. 核心命题：顺序 > 菜单

这是整套设计的出发点。

### 为什么「挑一个」必然出 AI 味

让模型从 N 个风格里「挑一个最适合的」，会发生什么？模型的训练目标让它倾向于**高概率、低风险、符合大多数样本**的答案。结果是：无论需求是什么，它都会向**分布的中心**收敛。

**菜单式选型的数学后果就是均值化。**

### 排除法为什么更好

改成「用约束砍掉不适用的」，剩下的不是**选**出来的，是**剩**下来的。

举一个真实的例子：需求是「一个修鞋摊，我不会电脑，改价格最好改个字就行，手机上能看」。

- **菜单式**：40 个风格里哪个最适合修鞋摊？模型会答「简洁朴素的」→ 落到某个中间值。
- **排除式**：维护者非技术（D3）→ **排除一切 Node 栈**；单页无交互 → **排除 vite / next / nuxt**；数据极简 → **排除 hugo**。剩下来的是 `plain-html`，产物是 `index.html` + 样式表，双击即开。**这不是选出来的，是被排除剩下的。**

排除法的另一个好处：**每条排除都能写出理由**，而理由可以被证伪。

### 但排除法有它自己的边界

**排除法只看当前这一单。** 当一批站的需求画像相似（小型 / 单页 / 无后端 / 中文 / 低频更新）时，排除法砍掉的是**同一批**选项，剩下的必然落在同一个区域。

这就是 §5 要讲的核心洞察。

---

## 3. 四层结构

这是最该先建立的框架。整套 skill 只有四种东西，职责不重叠：

| 层 | 形式 | 回答的问题 | 为什么用这种形式 |
|---|---|---|---|
| **散文层** | 15 个 `.md` | agent **该怎么想** | 决策逻辑用散文，agent 推理最稳 |
| **数据层** | 4 个 `.json` | **有什么可挑** | 结构化、可枚举、按需查、不整体进上下文 |
| **计算层** | 10 个 `.py` | **具体是多少** | 确定性工作交给代码，不靠手感 |
| **判据层** | 4 道自检 | **错了会不会被发现** | 没有判据的规则等于没有规则 |

### 最关键的一条设计宪法

> **凡能被枚举的，就不要写散文。**

想加一个「可选项」时，先问自己：它能不能变成 JSON 里的一行？

- **能** → 放数据层。比如「底色有哪些档」→ `_surfacePresets` 的 7 个键。
- **不能** → 放散文层。比如「什么时候该用排除法而不是菜单法」→ 这是推理，不是枚举。

**为什么这条这么重要**：一旦把可枚举的东西写成散文，agent 就会「综合一下给个中庸答案」——这就是 §2 说的均值化。

### 判据层为什么必须独立存在

因为**写下来的规则和执行时的行为之间，永远有缝**。

真实案例：`layouts.md` 白纸黑字写着「视觉签名必填」，但 `scripts/audit_tokens.py` 里**没有对应的检查**。结果 7 个站 5 个缺失视觉签名，而所有门禁都是绿的。

> **没有判据的「必填」等于不必填。**

而且还有更深一层：

> **一个永远返回 PASS 的判据，看起来和好判据一模一样。**

所以判据本身也要被测试——这就是 `testing/negative_tests.py` 存在的原因（见 §7）。

---

## 4. 名词表

这一节是全文最实用的部分。**「云里雾里」通常不是因为逻辑复杂，而是因为名词没对上。**

### 4.1 选型维度（决定「长什么样」）

按**收敛顺序**排列，每一步都在缩小空间：

| 名词 | 英文 / 字段 | 取值空间 | 在哪定义 | 作用 |
|---|---|---|---|---|
| **交付形态** | `host` | `standalone` / `embedded` / `system-only` | [`decide.md`](decide.md) | 独立站 / 嵌进已有站 / 只出 token。**形态不同，后半程全不同** |
| **技术栈** | `stack` | 7 个候选 | [`techstack.md`](techstack.md) + `scripts/pick_stack.py` | 决定「哪些风格做得出来、字体怎么加载」 |
| **信息架构** | `ia` | 4 个原型 | `architectures/` | 「由哪几页组成」，**优先于风格** |
| **风格族** | `family` | 11 个 | `styles/families.md` | 收敛用的中间层。族由**内容支撑力 / 转化目标 / 密度诉求**决定，**不由品类决定** |
| **风格锚点** | `slug` | **40 个** | `styles/index.json` | 最终落定的那一个风格 |
| **布局原型** | `layout` | 5 个 | [`layouts.md`](layouts.md) | 页面骨架。锚点绑定它，**风格必须驱动布局，不能只驱动颜色** |
| **中轴** | `axis` | `left-rail` / `center-axis` / `split` | [`layouts.md`](layouts.md) | 所有区块的对齐基准 |

**两级收敛**：约束 → **族（11 选 1–2）** → 锚点 → 1 个 spec。**先选族，再选锚点**，不许直接从 40 个里挑。

### 4.1b 内容维度（决定「分成几页、怎么在里面走」）

这一组是 **v0.9 补上的 IA 后半**。它和上面那组有个根本区别：

> **上面那组是「选」出来的（排除法），这一组是「算」出来的（判据法）。**

内容有多少条、有多深，是**客观事实**，不该拿来问用户（问了只会得到一个随口答案），
而应该由脚本读事实、算结论。所以这两个维度**不进约束筛选表**。

| 名词 | 英文 / 字段 | 取值空间 | 在哪定义 | 作用 |
|---|---|---|---|---|
| **页面粒度** | `granularity` | `single` / `master-detail` / `multi-page` / `n-a` | `architectures/pagination.json` + `architectures/granularity.md` | 内容**分成几页**：一个 `index` / 索引 + 详情 / 按主题分目录 / 不搭站 |
| **页内导航** | `inPageNav` | `none` / `anchor-jump` / `sticky-toc` / `section-rail` | 同上 + [`layouts.md`](layouts.md) | 长页里**怎么跳到关键部位**：不做 / 行内目录 / 吸顶目录 / 常驻侧栏 |

**两条关键分工**（容易搞反）：

- **档位由内容决定，形态由布局决定**。`pick_pages.py` 只算「该用哪一档」；
  具体做成行内目录还是吸顶条，由 [`layouts.md`](layouts.md) 的布局原型决定。
- **粒度由判据算，不占提问预算**。`intake.md` 的 B11 问的是**内容规模**（条目数、深度、是否要分享单条），
  不是「你要单页还是多页」。

**为什么不能用排除法**：排除法在批量场景下会**同向收敛**（见 §5）。需求画像相似时，
「排除」会砍掉同一批选项 —— 五个站会同时变成 `single + none`，正是 v0.8 之前的真实状态。
所以这一组走**判据法**：条件成立就升档，条件不成立就保持，和「别的站怎么选」无关。

### 4.2 数值维度（决定「具体是多少」）

| 名词 | 英文 / 字段 | 取值空间 | 在哪定义 | 作用 |
|---|---|---|---|---|
| **设计参数签名** | `signature` | 15 个数值参数 | `styles/signatures.json` | 把风格**数值化**：`r`（字阶公比）/ `base`（正文基准）/ `lh` / `wrap` / `ch` / `unit` / `trk` / `hue` / `chr` / `warm` / `rad` / `theme` / `mot` / `wts` / `disp` |
| **底色档** | `surface` | **7 档** | `styles/signatures.json` 的 `_surfacePresets` | `white` / `paper` / `tint` / `stone` / `slate` / `ink` / `deep`，含 2 档深底 |
| **尺度变体轴** | `variant` | **4 条** | `styles/signatures.json` 的 `_variantAxes` | `a` 原值 / `b` 收紧 / `c` 放松 / `d` 张扬，对 `r`/`base`/`unit`/`rad` 做**带 clamp 的偏移** |
| **字体对** | `pair` | 0–3 | `styles/fonts.json` | `0` = 锚点主选；`1–3` = 从该族备用池取第 n 对 |
| **族备用池** | `_familyPools` | 11 族 × 3 对 | `styles/fonts.json` | 解决「同锚点第二次使用无字可用」 |

**这四个数值维度就是反同质化的全部旋钮。** 组合空间见 §6。

### 4.3 ⚠️ 一词两义：两个「签名」

**这是最容易搞混的地方，务必分清。**

| 中文 | 英文 | 在哪 | 指什么 | 谁检查 |
|---|---|---|---|---|
| **设计参数签名** | signature | `styles/signatures.json` | **15 个数值参数**（`r` / `base` / `hue` …） | `audit_tokens.py` 的 **F5**（与锚点签名一致性） |
| **视觉签名** | visual signature | [`layouts.md`](layouts.md) | **≥3 条结构级差异化手法**（对角切分、悬挂标签、可见基线网格…） | `audit_tokens.py` 的 **F10**（存在性） |

两者**没有任何关系**：

- 「设计参数签名」是**数值**——它是「字阶公比 1.333」这种可计算的量。
- 「视觉签名」是**结构手法**——它是「价格列等宽右对齐」这种能在渲染结果里**指出来**的东西。

**为什么必须分开**：因为只调数值不够。两个站可以都用 `r = 1.333`，观感却完全不同；也可以都用同一个布局原型，却因为签名不同而一眼可辨。

> **视觉签名的验收标准**：把页面转成灰度截图，签名仍然成立 → 是真结构签名；消失 → 那只是配色，不是签名。

`layouts.md` 给每个布局原型准备了 **8 条候选签名**，选 ≥3 条并实际做出来。组合空间 `C(8,3) = 56`，足够一批站不撞。

### 4.4 流程概念

| 名词 | 含义 | 在哪 |
|---|---|---|
| **铁律** | **价值观**。为什么必须这样做。1–34 条，分五组：选型（1–11）/ 执行（12–21）/ 落地与审美（22–26）/ 反同质化（27–32）/ 可导航性（33–34） | [`SKILL.md`](SKILL.md) |
| **阶段门** | **检查点**。什么时候必须停下来验。G-1 → G0 → G1 → G1.5 → G1.6 → **G1.7 ★** → **G2 ★** → G3 → G4 → G5 → G6 | [`SKILL.md`](SKILL.md) |
| **决策权** | 每个决定归到 A 自主 / B 推断+确认 / C 必须问 / D 绝不代做。**判据：错了的返工成本 × 我有没有可靠依据** | [`decide.md`](decide.md) |
| **工作量分级** | L0 出系统 / L1 单页 / L2 多页嵌入。**只决定跑多少仪式，不决定质量门** | [`SKILL.md`](SKILL.md) |
| **证伪条件** | 每条选型结论必须写出「出现什么现象就说明我选错了」。**理由必须可证伪** | [`intake.md`](intake.md) / `styles/families.md` |

**铁律 ≠ 阶段门。** 铁律回答「为什么」，阶段门回答「什么时候查」。铁律是给 agent 读的原则，阶段门是流程上的卡点。

### 4.5 判据与批次概念

| 名词 | 含义 |
|---|---|
| **F / I / S / N 分级** | F 致命（已是缺陷，不许交付）/ I 重要（大概率错，不改要写明理由）/ S 建议（信息记录）/ N 命名（用了同义词，能用但难复用） |
| **批次** | 同一轮里交付的一组站。**「跨站差异」只在这个尺度上才有意义** |
| **批次账本** | `.style-ledger.json`（**项目侧，不进 skill 仓库**）。记住「上一次用了什么」，供下一站开工前查重 |
| **跨站差异度** | `audit_tokens.py --batch` 的 F11：底色唯一率 / display 唯一率 / **中轴不塌缩**（≥2 种且单档 ≤60%）/ 锚点唯一率 / 族比值 |

### 4.6 ⚠️ 编号歧义：同一个 F 号在两个脚本里含义不同

**两个脚本各自维护一套独立的 F 编号，F1–F6 是重号的。**

| 编号 | 在 `scripts/audit_tokens.py` 里 | 在 `scripts/validate_skill.py` 里 |
|---|---|---|
| F1 | `--axis` 存在且合法 | `signatures.json` 存在且可解析 |
| F2 | 对比度（WCAG） | `_legend` 覆盖 15 个字段 |
| F3 | 字阶等比、级数 ≤6、正文 ≥14px | 签名覆盖 `index.json` 全部锚点 |
| F4 | 间距落在同一基数整数倍 | 11 个族都有默认值 |
| F5 | 与锚点签名一致 | 签名各字段合法（枚举/量纲/区间） |
| F6 | **字体搭配**（反同质化） | 每个 spec 都有对应签名 |
| F7–F9 | —（未使用） | examples 过 audit / 字体注册表 / 技术栈+页面粒度选型 |
| F10 | **视觉签名存在性** | 底色维度（`_surfacePresets`） |
| F11 | **批次差异度**（`--batch`） | 尺度变体轴（`_variantAxes`） |
| F12–F13 | — | 族备用池 / 反同质化工具存在 |
| **F14** | **页面粒度一致性**（`--site`） | —（该段编号留给 H） |
| **F15** | **页内导航可达性**（`--site`） | — |
| **F16** | **字段渲染完整性**（`--site`） | — |
| **F17** | **内容可溯源**（`--site`） | — |
| **F18** | **一份真源**（`--site`） | — |
| **F19** | **嵌入服从宿主**（`--site`） | — |
| **F20** | **产物形状反映技术栈**（`--site`） | — |

**引用规则：说 F 号时必须带脚本名。** 例如「`audit_tokens.py` 的 F6」和「`validate_skill.py` 的 F6」是两件事。

F14–F20 都只在 **`--site <站点目录>`** 模式下跑——它们查的是**站点结构**，不是单个 token 文件。

| 编号 | 一句话 | 对应铁律 |
|---|---|---|
| F14 | `content-profile.md` 声明的粒度 与 实际页面文件数一致（`single`=1 / `master-detail`≥2 / `multi-page`≥3）。**声明缺失即失败** | 铁律 33 |
| F15 | 声明 ≠`none` 时须有 ≥2 个**可达**锚点；声明 `none` 而区块 ≥5 → 失败（=「一翻到底」）；另查全站锚点断链 | 铁律 34 |
| F16 | `src/data/*.json` 的数组字段若被代码只取 `[0]`，须另有 `.map()` / `.flatMap()` 渲染其余项。**只认 `map`/`flatMap`，不认 `forEach`** | 铁律 19 |
| F17 | `source-map.md` 表格行 ≥50% 带溯源标记。**文件缺失只报 I 级** | 铁律 18 |
| F18 | `MASTER.md` 与 `src/styles/tokens.css` 共有 token 值一致（比较前归一化） | 铁律 26 |
| F19 | 声明 `embedded` 须有 `HOST.md`，且共有 token 与宿主一致 | 铁律 14 |
| F20 | 选了 `plain-html` 就不能出现 `astro.config.*` / `src/pages/`，反之亦然 | 铁律 29 |

另外 `audit_tokens.py` 还有一个 **F0**：没解析到任何 CSS 自定义属性（说明喂错文件了）。

**为什么 `audit_tokens.py` 的新判据从 F14 起步（跳过 F12/F13）**：
正是因为上面这套「独立编号」——`validate_skill.py` 已经占了 F12/F13。
若 `audit_tokens.py` 也从 F12 开始编，就又多出一组重号，引用会更乱。
**新判据一律往后排**，把冲突面控制在已知范围内。

`validate_skill.py` 除 F 段外还有 **H 段**（v0.9 新增），三条都是**仓库层**判据（管 skill 自己）：

| 编号 | 查什么 | 抓的是什么问题 |
|---|---|---|
| **H1** | 四个 IA 文档写的「默认粒度 / 默认导航」与 `pagination.json` 的 `iaDefaults` 一致；注册表含 `falsify` 段 | **文档漂移**（改了注册表忘了改文档，或反之） |
| **H2** | `examples/` 下每个示例跑一遍 `audit_tokens.py --site`，必须 F 级 0 | 示例结构与判据脱节 |
| **H3** | 每档 `axis` 须 ≥3 个锚点、跨 ≥2 个布局原型、跨 ≥3 个族 | **维度没正交**：某档 100% 绑定单一布局时，选它就等于选布局 |

相对地，**F14–F20 是产出层**判据（管 agent 做出来的站）。H 段用字母而不是继续用 F，
正是为了避开上面的重号问题——**仓库层和产出层的命名空间必须分开**。

---

## 5. 核心洞察：单站自洽 ≠ 批次多样

**这是整套设计里最重要的一条，也是 v0.8 全部改动的出发点。**

### 问题

所有判据如果都只检查「单个站内部是否自洽」，会发生什么？

每个站单看都合格，**五个摆一起就塌**。因为没有任何一个脚本知道「上一次用的是什么」。

### 为什么排除法救不了

排除法的工作方式是「给一个需求，砍掉不适用的选项」。

当一批站的需求画像相似时——比如都是「小型 / 单页 / 无后端 / 中文 / 低频更新」——**排除法砍掉的是同一批选项**，剩下的必然落在同一个区域。

**补法不是改排除法，而是加一个维度：记住上次用了什么。**

### 三道防线

| 防线 | 时机 | 机制 | 检查什么 |
|---|---|---|---|
| **事前** | 开工前 | `scripts/ledger.py --check` | 本次选型（锚点/底色/中轴/字体）是否与同批次已交付的站撞车，并**给出替代组合** |
| **单站** | 交付前 | `audit_tokens.py` 的 F6 / F10 | 字体是否做了搭配、视觉签名是否 ≥3 条 |
| **跨站** | 一批做完 | `audit_tokens.py --batch` 的 F11 | 底色唯一率 / display 唯一率 / 中轴不塌缩 / 锚点唯一率 / 族比值 |

三道防线缺一不可：

- 只有单站 → 数值全过、观感全同（这就是原来的状态）。
- 只有跨站 → 知道「撞了」，但不知道「本来该选什么」。
- 只有事前 → 事后没有复核，账本可能记错。

### ⚠️ 一次判错方向的修正：阈值要按档数归一化

中轴的阈值曾经定成 **`MIN_AXIS_KINDS = 3`**，而中轴总共就 3 档——
要求「3 种全部出现」等于要求覆盖率 **100%**，那是**配额**，不是多样性。

对比一下就清楚了：

| 维度 | 总档数 | 阈值 | 要求覆盖率 | 性质 |
|---|---|---|---|---|
| 底色 | 7 档 | ≥3 种 | **43%** | 多样性 ✓ |
| 中轴 | 3 档 | ≥3 种（旧） | **100%** | 配额 ✗ |

**同一个数字「3」，在档数不同的维度上含义完全相反。** 后果很具体：
每个批次都被强制包含一个 `split` 站，而 `split` 当时只有 3 个锚点、全挤在一个布局原型里——
**中轴把布局与族的自由度一起锁死了**。

修正后的语义（现在的实现）：

```
中轴 ≥2 种  且  单档占比 ≤60%
```

它仍然抓得住「5 站全 `left-rail`」这种真塌缩，但不再制造必经点。
这条判据的名字也从「中轴种类」改成更准确的**「中轴不塌缩」**。

> **教训：阈值必须按档数归一化。** 定阈值时先问「总共有几档」，
> 再决定「要出现几档」。只写数字不写比例，迟早会把多样性做成配额。

### 一个必须警惕的陷阱：假通过

`ledger.py --report` 曾经在未记录 `--font-display` 的情况下，把 display 唯一率显示成 `0/5 = 100%`。

**没记录 = 这一项没被校验 = 不通过。** 不能显示成 100%。现已改为判「未记录」不通过，并在记账缺该参数时给出提示。

> 这条和「没有判据的必填等于不必填」是同一个坑的两副面孔。

---

## 6. 组合空间：40 个锚点为什么够用

如果只有 40 个锚点，一批站很容易撞。加五个维度之后：

```
40 锚点  ×  4 变体轴  ×  7 底色档  ×  4 字体对  ×  3 粒度档  ×  4 导航档
                                              =  53760 种组合
```

（前四项 = 4480，是 v0.8 的空间；后两项是 v0.9 补的。）

再叠加布局原型的 8 选 3 签名（`C(8,3) = 56`），实际空间远大于此。

**这不是「多给几个选项」，而是「让同一套设计语言可以被重新表达」。** 同一个锚点第二次使用时，换底色、换变体、换字体对、换粒度、换签名组合——它仍然是那个锚点，但不再是同一份交付。

**但空间大不等于必须用满。** 页面粒度和页内导航是**内容驱动**的：
一个 3 区块的咖啡店页加目录、一个 4 件作品的作品集硬拆详情页，都是**负优化**。
所以这两个维度是**算出来的**，不是「尽量多加」的——见 §4.1b。

### 约束

新维度必须**带安全边界**，否则会产出畸形结果：

- 变体轴 `dr` 绝对偏移 ≤ 0.25（超过会让字阶失真）
- `dunit` ∈ [0.3, 3.0]、`dbase` 绝对偏移 ≤ 3、`drad` 绝对偏移 ≤ 12
- 字阶公比最终落在 `r ∈ [1.15, 1.62]`、`base ∈ [13, 20]`、`unit ∈ [2, 24]`、`rad ∈ [0, 24]`

这些 clamp 在 `scripts/emit_tokens.py` 里，由 `validate_skill.py` 的 F11 校验。

---

## 7. 判据体系

### 五个检查对象，五道自检

| 自检 | 检查对象 | 什么时候跑 | 通过标准 |
|---|---|---|---|
| `scripts/validate_skill.py` | **skill 仓库自己** | 改了 skill 的任何文件 | `ERROR 0` |
| `scripts/audit_tokens.py <MASTER.md>` | **一份产出的数值** | 交付前 | `F 级 0` |
| `scripts/audit_tokens.py --site <站点目录>` | **一份产出的结构** | 交付前 | `F 级 0` |
| `scripts/audit_tokens.py --batch <目录>` | **一批产出之间** | 一批做完后 | 无 FAIL |
| `testing/negative_tests.py` | **判据本身** | 加了/改了任何判据 | 57/57 全抓到 |

**它们不是四个难度，是四个不同的检查对象。** 搞混会导致「跑了自检却漏了问题」。

`--site` 与不带参数的 `audit_tokens.py` 是**同一份产出的两个面**：
不带参数查**数值**（字阶、对比度、字体搭配），`--site` 查**结构**
（页面粒度对不对、页内导航的锚点是否可达）。数值合格但结构是单个 `index`，一样不合格。

### `validate_skill.py` 的段落划分（A–H）

| 段 | 查什么 |
|---|---|
| **A** | frontmatter 是否符合 Agent Skills 规范（A1–A6） |
| **B** | 结构（SKILL.md 行数 < 500、声明了 license） |
| **C** | 引用自洽（C1 链接存在 / C2 文档互引可解析 / C3 孤儿文件） |
| **D** | 仓库卫生（无构建产物、无一次性脚本、无用户源材料） |
| **E** | 注册表自洽（E1 锚点的 layout/family/axis 落在定义域内 / E2 孤儿 spec / E3 示例声明 `--axis`） |
| **F** | 设计参数签名与产出审计（F1–F7） |
| **G** | 字体 / 技术栈 / 反同质化基础设施（F8–F13） |
| **H** | 页面粒度与页内导航 + 中轴供给（**H1** IA 文档与注册表默认值一致 / **H2** 示例过 `--site` / **H3** 中轴档位供给正交） |

**E 段专门抓「文档互相矛盾」**。这类漂移单看任何一个文件都自洽，只有交叉比对才暴露，靠人眼审查必然漏。
**H1 是同一个思路的新实例**：`pagination.json` 的 `iaDefaults` 与四个 IA 文档里写的默认值，
改了一边忘了另一边就漂移 —— 这正是「改注册表忘了改文档」的典型现场。

### `negative_tests.py`：为什么判据本身要测

> **正向测试只能证明「不误报」，证明不了「能报错」。**

一个永远返回 PASS 的判据，在回归用例里看起来永远「全绿」，和好判据一模一样。

`negative_tests.py` 的做法：给每条判据**植入一处确定的违规**，断言它被抓到。覆盖 57 条，分七组：

| 组 | 覆盖 |
|---|---|
| `validate_skill.py` | F8 / F9 / F10 / F11 / F12 / F13 / **H1** / **H3** / **H4** |
| `audit_tokens.py`（单站） | F6 / F10 |
| **`audit_tokens.py --site`** | **F14 / F15 / F16 / F17 / F18 / F19 / F20** + 三个示例与**黄金站点**的正向防误报 |
| `scripts/ledger.py` | 事前查重 + 批次报告（含中轴占比） |
| `audit_tokens.py --batch` | F11 跨站差异度 |
| **端到端** | `emit_tokens.py` 真出 5 站 token → `--batch` 必须 PASS |

**每条用例同时包含负向（违规必须被抓）与正向防误报（合规必须通过）两类。** 只测前者会把判据越改越严，最后正常产出也被拦下。

### 7.1 写判据的三个反直觉教训（都是真实踩出来的）

#### 一：判据写松了等于没写

F16（字段渲染完整性）最初把 `forEach` 也算「渲染过」。结果负向用例退出码 0、**一条都没抓到**——
因为示例的灯箱脚本里本来就有 `images.forEach(...)`，于是「脚本遍历过」掩盖了「模板只渲染第一张图」。

收紧为**只认 `.map()` / `.flatMap()`（产出标记），不认 `forEach`（只做副作用）**之后才生效。

> **判据的严格程度决定了它是否存在。** 写松的判据在测试里表现为「全部通过」，和好判据一模一样。

#### 二：正向防误报要专门找盲区

F17/F19/F20 在三个示例上**根本不触发**——示例没有 `source-map.md`、也没声明 host/stack。
只跑示例会误以为判据没生效。

为此专门造了两个东西：

| 名字 | 是什么 | 补什么盲区 |
|---|---|---|
| **`GOLD_SITE`** | 处处合规的**最小站点**（在临时目录里合成） | F17 / F19 / F20 的**正向路径**——证明它们不会误伤合规站点 |
| **`PIPE-all-anchors`** | 40 个锚点**逐个**跑 `emit_tokens` + 单站审计 | 把「被验证过的锚点」从 3/40（7.5%）拉到 **40/40** |

> **先问「这条判据在现有素材上会触发吗」，不会就自己造素材。** 永远返回 PASS 的判据不是没工作，是没被测到。

#### 三：别用加内容的方式解决可计算的问题

「示例只覆盖 3/40 个锚点」这个缺口，最直觉的补法是再建 37 个示例站点——
但示例是「输出形状的参考」，再建一个要真内容、要维护构建产物，成本极高。

而「每个锚点都能产出合格 token」是**可以机器全量验证**的事。
`PIPE-all-anchors` 逐个 emit + 审计 40 个锚点，约 20 秒，一次把覆盖率从 7.5% 拉到 100%。

> **遇到「覆盖不够」，先问它能不能被算出来。** 能算的就别堆内容。

---

## 8. 四条元规则

如果你只记住四件事，记这四条：

### 规则一：凡能被枚举的，就不要写散文

散文会诱导 agent「挑一个平均物」。可枚举的东西一律进 JSON 注册表。

### 规则二：任何「必填」都必须配机器判据

**没有判据的必填 = 不必填。** 视觉签名就是这么失效的——`layouts.md` 写了必填，但没人检查，于是 5/5 缺失而门禁全绿。

### 规则三：加了判据，必须配负向测试

**永远返回 PASS 的判据等于没有判据。** 正向测试只能证明「不误报」，证明不了「能报错」。
而且**判据写松了也等于没写**（F16 把 `forEach` 算作「渲染过」的教训，见 §7.1）。

### 规则四：定期用规则反审规则本身

前三条会**持续失效**，因为写铁律比写判据容易得多——铁律永远领先于判据。
v0.9.1 审计时发现：34 条铁律只有 14 条（41%）有机器判据，
其中 5 条是本可机器化却没做的（铁律 14/18/19/26/29）。

反审方法（不需要读代码，只需要提问）：

> 逐条看铁律，对每条问：**「违反它的最小产物长什么样，有哪个脚本会报错？」**
> **答不出脚本名的，就是欠账。**

这个方法也适用于任何一个「有规矩但没人查」的场合——它是这套 skill 唯一一条**反身**的规则。

---

## 9. 与同类工具的差别

| 维度 | TypeUI / Curio / uipro 等 | 本 skill |
|---|---|---|
| 解决的问题 | 「长什么样」 | **「按什么顺序定」** |
| 选型方式 | 从 N 个里挑 1 个 | 约束**排除**掉 80%，剩下的才是答案 |
| 布局 | 共享模板换色 | 锚点**驱动**布局（`layout` + `axis` 是一等公民） |
| 信息架构 | 通常不管 | IA 优先于风格，且**两半都管**：区块组成（`architectures/` 四选一）+ **页面粒度** |
| 页面粒度 | 默认单页 | **判据算出来的**（`pick_pages.py`），可拆可不拆，不搞一刀切 |
| 页内导航 | 不做 | 长页给**可达锚点**，档位由内容定、形态由布局定 |
| 技术栈 | 默认继承框架 | **Phase 1.5 独立判断**（`pick_stack.py`） |
| 字体 | 常用栈兜底 | 40 套具名搭配，**display 全库唯一** |
| 数值 | 手感 | 15 参数签名 → **确定性展开 + WCAG 求解** |
| 验收 | 人眼 | 三层次判据 + **判据自身的负向测试** |
| 多站一致性 | 不涉及 | **批次账本 + 跨站差异度** |

**本 skill 不替代它们。** `_specFallback` 里明确写了：本地 spec 不够时可以外接 `npx typeui.sh pull <slug>`（67 种）。差别在于——**它是被本 skill 排除法选出来的候选，而不是你随手挑的。**

---

## 10. 术语速查表

| 中文 | 英文 / 字段 | 一句话 |
|---|---|---|
| 锚点 | anchor / `slug` | 最终落定的那一个风格（40 选 1） |
| 风格族 | family | 收敛中间层（11 选 1–2），由内容支撑力决定，**不由品类决定** |
| 布局原型 | `layout` | 页面骨架（5 选 1） |
| 中轴 | `axis` | 对齐基准（3 选 1） |
| 设计参数签名 | signature | 锚点的 **15 个数值参数** |
| 视觉签名 | visual signature | **≥3 条结构级手法**，灰度下仍成立 |
| 底色档 | `surface` | 7 档底色（含 2 档深底） |
| 尺度变体轴 | `variant` | 4 条尺度偏移（原值/收紧/放松/张扬） |
| 字体对 | `pair` | 0 主选，1–3 取族备用池 |
| 交付形态 | `host` | standalone / embedded / system-only |
| 技术栈 | stack | 7 个候选，由需求排除 + 定序得出 |
| 信息架构 | `ia` | 4 个原型，优先于风格 |
| 页面粒度 | `granularity` | 内容分几页（single / master-detail / multi-page / n-a），**算出来的** |
| 页内导航 | `inPageNav` | 长页怎么跳（none / anchor-jump / sticky-toc / section-rail） |
| 阶段门 | G-1 … G6 | 11 个检查点，G2 是唯一强制人工确认点 |
| 铁律 | — | 34 条价值观，分五组 |
| 决策权 | A/B/C/D | 自主 / 推断确认 / 必须问 / 绝不代做 |
| 判据分级 | F/I/S/N | 致命 / 重要 / 建议 / 命名 |
| 批次账本 | ledger | 记住「上次用了什么」，事前查重 |
| 跨站差异度 | — | 一批站之间的差异指标（F11） |
| 证伪条件 | — | 「出现什么现象说明我选错了」 |

---

## 附：一份 60 秒的自我检查清单

想确认自己真的理解了，试着回答这八个问题：

1. 「设计参数签名」和「视觉签名」分别指什么？谁检查它们？
2. 为什么 `layouts.md` 写了「视觉签名必填」，7 个站还是 5 个缺失？
3. 排除法在什么情况下会失效？补法是什么？
4. 同一个锚点第二次使用，有哪几个旋钮可以换？
5. `validate_skill.py` 的 F6 和 `audit_tokens.py` 的 F6，分别查什么？
6. 为什么「页面粒度」不能用排除法选，只能算？它和「风格锚点」的判断机制差在哪？
7. 中轴阈值曾经定成「≥3 种」，为什么那是**配额**而不是多样性？现在改成什么了？
8. 34 条铁律只有 14 条有机器判据——这个问题该怎么发现？发现之后怎么补？

答得出来 → 你已经掌握这套东西了。答不出来 → 回 §4.3、§3、§5、§6、§4.6、§4.1b、§5 末尾、§8 规则四。
