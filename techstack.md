# Tech Stack — 技术栈选型（Phase 1.5）

> **顺序**：约束筛选（G1）之后、落定风格与字体（G2）之前。
> **为什么必须在风格之前**：技术栈决定「哪些风格做得出来、哪些做不出来」。先选风格再选栈，等于先承诺效果再发现实现不了 —— 返工最贵的一类。
> **为什么必须在字体之前**：字体加载方式（自托管 / CDN / 系统回退）由栈决定。选了 `plain-html` 就不可能用 Next 的 `next/font` 自动子集化。

> ⚠️ **这一阶段的存在本身，是对一个真实失败模式的修法**：
> 用「AI 生成网页」时，模型会不假思索地输出「Astro 项目 + `src/pages/index.astro` + `src/styles/tokens.css` + `src/data/*.json`」这一套骨架，
> 于是无论什么内容，产出的都是**同一个形状换文案** —— 本质上是「不同的 Markdown 组织器」。
> 技术栈必须是**被判断出来的**，不是被默认继承的。

---

## 给 agent 的用法

1. **先收集输入**（下表 8 项），能从 `intent-summary.md` / Grill 推断的就推断，不要重复问。
2. **跑一次确定性推荐**：`python scripts/pick_stack.py --pages ... --interaction ...`（见 §4）。它给排序后的候选栈 + 理由 + 被排除项。
3. **写 `tech-stack.md`**（输出契约见 §5），含**证伪条件**。
4. **并入 G2 落定一起确认**（技术栈不单独设人工确认点，它和 IA / 锚点 / 中轴 / 字体一起点头）。
5. 确认后才进 Phase 2 与 Phase 4。

---

## 1 · 输入的 8 个判定项

| # | 判定项 | 取值 | 为什么它决定栈 |
|---|---|---|---|
| D1 | **页面规模** | `single` 单页 / `few` 2–6 页 / `many` 7+ 页或大量条目 | 单页不需要路由框架；多页需要模板复用 |
| D2 | **交互复杂度** | `none` 纯展示 / `light` 灯箱·筛选·主题切换 / `heavy` 图表·动画·拖拽·状态机 | 重交互要构建工具与状态管理，静态生成器会碍事 |
| D3 | **维护者技术程度** | `nontech` / `tech` / `team` | 非技术维护者 → 零构建或极简构建；改一个 JSON 就能更新 |
| D4 | **内容数据源** | `inline` 硬编码 / `single-json` 单一数据文件 / `cms` / `api` | 决定是否需要构建期数据层或运行时请求 |
| D5 | **交付形态** | `standalone` / `embedded` / `system-only` | `embedded` → **服从宿主栈**，本阶段只做确认不做选择 |
| D6 | **多语言** | `none` / `multi` | 路由级 i18n 需要框架支持，纯静态要手写多套页面 |
| D7 | **渲染需求** | `static` 纯静态 / `ssr` 需服务端（个性化、鉴权、实时） | SSR 直接排除所有纯静态方案 |
| D8 | **性能预算** | `lean` 首屏极敏感（< 100KB JS） / `normal` | 决定要不要上框架运行时 |

> **`embedded` 是短路分支**：D5 = `embedded` 时，D1–D4、D7 全部由宿主决定 —— 你**只读宿主栈**（`host-align.md`），不另起一套。技术栈选型的输出退化为「确认宿主栈 + 说明本页如何在该栈内落地」。

---

## 2 · 候选栈画像（7 个）

每个栈给出：本质、最适合、**不适合**、代价。选型就是在「不适合」里排除。

### ① `plain-html` · 纯静态 HTML + CSS（± 原生 JS）
- **本质**：一个或几个 `.html` 文件 + 一个样式表 + 一个可选脚本。零构建、零依赖、双击即开。
- **最适合**：单页、内容极少或中等、交互 `none`/`light`、维护者 `nontech`、性能预算 `lean`、需要**能直接嵌进任何宿主**。
- **不适合**：多页（无模板复用，改一处要改 N 个文件）、`many` 条目、`multi` 多语言、`heavy` 交互、需要数据层。
- **代价**：超过 6 页后维护成本指数上升；SEO 结构化数据要手写。

### ② `astro` · Astro（内容站首选）
- **本质**：默认零 JS 的静态站点生成器，组件化 + islands 架构（只有需要交互的组件才注水）。
- **最适合**：`few`–`many` 页、内容驱动、`light` 交互、`multi` 多语言（原生 i18n 路由）、`single-json`/`cms` 数据源、`normal` 性能预算。
- **不适合**：`heavy` 交互的类应用（应上 Vite/Next）、维护者 `nontech`（需要 Node 与命令行）、纯单页（杀鸡用牛刀）。
- **代价**：引入 Node 构建链；非技术维护者改内容需先 `npm install`。

### ③ `eleventy` · 11ty
- **本质**：极简静态生成器，模板语言可选，产物是纯 HTML。
- **最适合**：`many` 页、内容以 Markdown/JSON 为主、几乎无交互、追求产物干净。
- **不适合**：需要组件化交互、`multi` 多语言（需自行搭路由）、`heavy` 交互。
- **代价**：生态与文档比 Astro 弱；复杂布局要自己写模板函数。

### ④ `vite-vanilla` · Vite + 原生 TS（+ 图表/动画库）
- **本质**：构建工具 + 原生代码，没有框架运行时。交互重但 UI 不复杂时的最优解。
- **最适合**：`single`/`few` 页但 `heavy` 交互（数据可视化、Canvas/WebGL、滚动动画、参数空间）、性能预算 `lean`。
- **不适合**：内容为主的站（写内容不如 Astro 舒服）、`many` 页。
- **代价**：路由、状态、i18n 都要自己搭；团队协作时约定成本高。

### ⑤ `next` · Next.js
- **本质**：React 全栈框架，SSR/SSG/ISR 一体，`next/font` 自动优化字体。
- **最适合**：`ssr` 需求（鉴权、个性化、实时数据）、`heavy` 交互 + 多页、`api` 数据源、需要 Server Actions。
- **不适合**：纯静态内容站（运行时与构建都重）、`lean` 性能预算、维护者 `nontech`。
- **代价**：依赖体积大、构建慢、版本升级频繁。

### ⑥ `nuxt` · Nuxt
- **本质**：Vue 版的 Next，能力对等。
- **最适合**：团队已有 Vue 技术栈时；其余同 `next`。
- **不适合**：同 `next`；且团队不熟 Vue 时不要为了「技术新」而选。
- **代价**：同 `next`。

### ⑦ `hugo` · Hugo
- **本质**：Go 写的静态生成器，构建极快，单二进制无 Node 依赖。
- **最适合**：`many` 页（数百上千）、内容更新频繁、无交互、构建速度敏感。
- **不适合**：需要组件化交互、`multi` 多语言虽支持但配置繁琐、`heavy` 交互。
- **代价**：模板语法（Go template）学习曲线陡；前端生态弱。

---

## 3 · 排除逻辑（不是「挑一个」，是「剩下来」）

按顺序排除 —— 与 `intake.md` 的约束筛选同构：

| 若成立 | 立即排除 | 原因 |
|---|---|---|
| D5 = `embedded` | 除宿主栈外的全部 | 服从宿主，见 `host-align.md` |
| D7 = `ssr` | `plain-html` `astro` `eleventy` `hugo` `vite-vanilla` | 纯静态方案做不了服务端渲染 |
| D3 = `nontech` | `astro` `eleventy` `next` `nuxt` `hugo` `vite-vanilla` | 非技术维护者不能碰命令行；只剩 `plain-html` |
| D2 = `heavy` | `plain-html` `eleventy` `hugo` | 重交互需要构建期模块系统与状态管理 |
| D1 = `many` | `plain-html` `vite-vanilla` | 无模板复用 → 维护成本爆炸 |
| D6 = `multi` 且无框架 | `plain-html` `hugo`（配置繁琐） | 路由级 i18n 需要框架支持 |
| D8 = `lean` 且 D1 = `single` | `next` `nuxt` | 框架运行时与首屏预算冲突 |

**排除后应剩 1–3 个候选**，再按下面的评分定序。若排除后**一个都不剩** → 说明输入自相矛盾（例如「非技术维护者 + 重交互 + SSR」），**回 G0 补问**，不是硬选一个。

---

## 4 · 确定性推荐

排除逻辑与评分已固化成脚本，**不要凭印象选**：

```bash
python scripts/pick_stack.py --pages single --interaction light --maintainer nontech \
    --data single-json --host standalone --i18n none --render static --budget lean
```

输出：排序后的候选栈（含总分与逐条理由）、被排除项（含被哪条判据排除）、以及一段可直接粘进 `tech-stack.md` 的结论。

参数与 D1–D8 一一对应，取值见 `--help`。**`--json` 输出可被其它脚本消费。**

---

## 5 · 输出契约：`tech-stack.md`

```markdown
# 技术栈选型

## 输入判定（D1–D8）
- D1 页面规模：single ｜ D2 交互：light ｜ D3 维护者：nontech
- D4 数据源：single-json ｜ D5 形态：standalone ｜ D6 多语言：none
- D7 渲染：static ｜ D8 性能预算：lean
（每项注明依据：来自哪条 Grill 答案 / 哪份源材料）

## 结论
选定：`plain-html`
因为：维护者非技术（D3）→ 排除一切需要 Node 的方案；单页 + 轻交互（D1/D2）→ 无需路由与状态管理；
      数据是单一 JSON（D4）→ 运行期 fetch 即可；性能预算 lean（D8）→ 框架运行时不可接受。

## 被排除项
- `astro` —— 被 D3 排除（非技术维护者无法运行 npm install / 命令行）
- `next` —— 被 D3 + D8 排除
- …（每条写明被哪条判据排除）

## 该栈的落地约束
- 构建命令：无（双击 index.html 即可预览）
- 内容更新方式：改 data/*.json
- 字体加载：自托管 woff2 + font-display: swap（见 styles/fonts.json 的 loading 字段）
- 部署：任意静态托管（含 GitHub Pages / 对象存储）

## 证伪条件（如果…就说明栈选错了）
1. 若需要新增第 7 个页面 → 说明 D1 判断错误，应回退到 `astro`
2. 若维护者提出要自己加一个筛选器 → 说明 D2 低估了交互，应评估 `vite-vanilla`
3. 若首屏 JS 超过 100KB → 说明引入了不该有的运行时，违反 D8
```

**证伪条件必须可观测**（能拿一个具体数字或具体事件检验），不许写「如果不好用」。

---

## 6 · 反模式（这些一出现就说明栈选错了）

| 反模式 | 症状 | 修正 |
|---|---|---|
| **默认继承 Astro** | 不管什么需求都生成 Astro 项目 | 跑 `pick_stack.py`；单页 + 非技术维护者应落到 `plain-html` |
| **为「技术先进」而选** | 用 Next 做一个 3 页的静态介绍站 | 框架运行时是负债不是资产；回到 D1/D8 |
| **形状同质化** | 不同项目的产物目录结构一模一样 | 栈决定目录形状：`plain-html` 是 index.html + 样式表，`astro` 才是 `src/pages/`。**形状相同 = 没做选型** |
| **跳过选型直接写代码** | 直接 `npm create astro` | 违反 Phase 1.5；未出 `tech-stack.md` 不许进 Phase 4 |
| **`embedded` 却另起一套栈** | 宿主是 Next，你却在页面里塞 Astro 组件 | 见 `host-align.md`，服从宿主 |

---

## 7 · 与其它阶段的关系

```
G1 约束筛选 ──→ ★ Phase 1.5 技术栈选型 ──→ G2 落定（IA + 锚点 + 布局 + 中轴 + 字体 + 技术栈）──→ G3 token
                        │                                    │
                        │                                    └─ 字体加载策略由栈决定（§5 落地约束）
                        └─ 栈反过来给风格加约束：
                           plain-html → 不能用依赖框架的动效库
                           vite-vanilla → 可做 kinetic-type / 数据可视化
                           astro/next → 可做多语言、可做组件化图库
```

技术栈不是「实现细节」，它是**风格可行域的一部分**。这就是它必须排在风格之前的原因。
