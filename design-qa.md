# Design QA — 可测量的审美判据

> `checklist.md` 管**流程与形状合规**（有没有做、走的对不对）；本文件管**数值**
> —— 页面好不好看里能被量化的那部分。多数条目由 `scripts/audit_tokens.py` 自动检查。
> 其中 **F6 / F10 / F11 是反同质化判据**：F6 查字体搭配（单站），F10 查视觉签名（单站），
> F11 查批次差异度（**跨站**，需 `--batch`）。F6 于 v0.7 新增，F10 / F11 于 v0.8 新增。
> **F14 / F15 是页面结构判据**（**`--site`**）：页面粒度与页内导航的「声明 vs 产物」是否一致，
> 于 v0.9 新增 —— 编号跳过 F12/F13 是为了避开 `validate_skill.py` 的同名编号。
> **F16–F20 是交付完整性判据**（同样 `--site`）：把 5 条「一直写在铁律里、却始终没有机器判据」
> 的要求补上 —— 字段必须渲染、内容可溯源、一份真源、嵌入服从宿主、产物形状反映技术栈。

```bash
python scripts/audit_tokens.py <MASTER.md 或 tokens.css>
python scripts/audit_tokens.py <MASTER.md> --anchor <slug>   # 加签一致性校验
python scripts/audit_tokens.py --site <站点目录>             # F14/F15 页面结构
python scripts/audit_tokens.py --batch <批次目录>            # F11 跨站差异度
```

退出码非 0 = 有 F 级未过 = **没做完**。

**判据本身也要被测试。** 改了本文件里的任何判据，都要验证它在违规时真的会报错：
它给每条判据植入一处确定的违规，断言对应检查码真的会报错。
一个永远返回 PASS 的判据等于没有判据 —— v0.7 的「视觉签名必填」就是这么失效的。

---

## 分级

| 级 | 含义 | 处理 |
|---|---|---|
| **F 致命** | 已是缺陷，不是风格选择 | 必须改；带病不许交付 |
| **I 重要** | 大概率是错的，但可能有理由 | 改，或在 MASTER.md 写明为何不改 |
| **S 建议** | 信息性记录，供人复核 | 看一眼，同意即过 |
| **N 命名** | 用了同义词而非规范名 | 能用，但跨项目复用会变难 |

---

## 判据

### F 级

| 编号 | 判据 | 目标 | 不过的修法 |
|---|---|---|---|
| **F1** | `--axis` 存在且合法 | `left-rail` / `center-axis` / `split` | 中轴未定就开工 = 返工最贵的一类；见 `layouts.md` |
| **F2** | 对比度（半透明色已合成到底色上） | `--fg` ≥7:1、`--muted` ≥4.5:1、`--accent-ink` ≥4.5:1、`--accent` ≥3:1 | 调明度，或让 `emit_tokens.py` 的 `solve_contrast` 重新求解 |
| **F3** | 字阶**等比**、级数 ≤6、正文 ≥14px、最大字号明显拉开 | 相邻比值偏离中位数 ≤10% | 用模块化字阶重算；见 `typography.md` §1 |
| **F4** | 所有 `--space-*` 是同一基数的整数倍 | 全部落在最小间距的整数倍上 | 混入栅格外数值是「说不出的不整齐」的主因 |
| **F5** | **与锚点签名一致**（`--anchor` 时启用） | 基准字号 / 公比 / 行高 / 行长 / 栅格 / 圆角 / 中轴与 `styles/signatures.json` 一致（公比容差 5%） | 有意偏离 → 在 MASTER.md 写明理由 |
| **F6** | **字体搭配**（反同质化） | `--font-display` / `--font-body` 均存在；display 首族是**具名字体**（非 `system-ui`/`-apple-system`/`Inter` 等通用关键字）；display ≠ body | 从 `styles/fonts.json` 取该锚点的搭配（`emit_tokens.py` 已自动落）；**字体没做搭配 = 40 个站长得一样**，这是「千篇一律」最直接的技术成因 |
| **F10** | **视觉签名存在性**（反同质化 · v0.8） | `MASTER.md` 含「视觉签名」段，且段内 ≥3 条结构级手法 | 见 `layouts.md` 的签名菜单（每原型 8 条候选）。对 `.css` 文件自动降级为提示（签名段属 MASTER.md）。**没有判据的「必填」等于不必填** —— 实测 5 个站 5/5 缺失 |
| **F11** | **批次差异度**（跨站 · `--batch` 时启用） | 底色值唯一率 ≥80%、**中轴 ≥2 种且单档占比 ≤60%**、display 唯一率 100%、基准字号 / 间距基数各 ≥3 种、圆角 ≥2 种 | 单站自洽 ≠ 批次多样。逐站跑 audit 可能全 PASS，摆一起才看得出塌。**实测 5 站：`--bg` 5/5 全 `#FFFFFF`、锚点只用 3 个**。中轴不写「≥3 种」：它总共就 3 档，要求全出现是配额不是多样性，会强制每批都含一个 `split` 站 |
| **F14** | **页面粒度**（结构 · `--site` 时启用） | 声明存在且取值合法（`single` / `master-detail` / `multi-page` / `n/a`）；**与实际页面文件数一致**（1 / ≥2 / ≥3） | 跑 `python scripts/pick_pages.py` 算出结论，写进 `content-profile.md`。**未声明即失败** —— 「所有站都只出单个 index」就是这么发生的：文档写了必填，却没有判据 |
| **F15** | **页内导航**（结构 · `--site` 时启用） | 声明存在且取值合法；≠`none` 时首页须有 ≥2 个**可达**锚点（`href="#x"` 有对应 `id="x"`）；`none` 时首页区块数不得 ≥5；全站无锚点断链 | 档位见 `architectures/granularity.md`，形态见 `layouts.md` 的「页内导航（按布局原型）」。**长页声明 `none` = 用户只能一翻到底** |
| **F16** | **字段渲染完整性**（铁律 19，v0.9.1） | `src/data/*.json` 里每个数组字段，若有代码只取 `[0]`，则须另有 `.map()` / `.flatMap()` 渲染其余项 | **只认 `map`/`flatMap`，不认 `forEach`** —— `forEach` 通常只做副作用（注册灯箱数据、绑事件），页面上一个元素都不会多。实测：`images.forEach(...)` 出现在灯箱脚本里，掩盖了模板只渲染第一张图 |
| **F17** | **内容可溯源**（铁律 18，v0.9.1） | `source-map.md` 的表格行中 ≥50% 带溯源标记（`p12 · "原句"` / URL） | **文件缺失只报 I 级**：不是所有项目都要交付溯源表，但不能「有表却全是编的」 |
| **F18** | **一份真源**（铁律 26，v0.9.1） | `MASTER.md` 与 `src/styles/tokens.css` 的共有 token 值必须一致 | 比较前做归一化（去空白、转小写）—— 字体栈 `Archivo","Helvetica` 与 `Archivo", "Helvetica` 只是逗号后空格差异，不能判漂移 |
| **F19** | **嵌入服从宿主**（铁律 14，v0.9.1） | 声明 `embedded` 时须有 `HOST.md`，且共有 token 与宿主一致 | `embedded` 是三种交付形态之一，此前零机检 |
| **F20** | **产物形状反映技术栈**（铁律 29，v0.9.1） | 选了 `plain-html` 就不能出现 `astro.config.*` / `src/pages/`；选了 `astro` 就必须有 | 否则 Phase 1.5 的技术栈选型等于白做 —— 结论没落到产物上 |

### I 级

| 编号 | 判据 | 目标 | 说明 |
|---|---|---|---|
| **I1** | 行长 `--prose-width` | 44–80ch（≈22–40 汉字/行） | `ch` 是半角单位；正文列宽用 ch/em，不用 px |
| **I2** | 行高 | 正文 1.4–2.0、标题 ≤1.3、且标题 < 正文；行高**必须无单位** | 行高随字号增大而收紧 |
| **I3** | 强调色数量 | 高饱和强调色 ≤2 | `accent`/`accent-soft`/`accent-ink` 同色系算 1 |
| **I4** | 字重数量 | ≤2 | 超过两档就失去克制 |
| **I5** | 圆角档数 | ≤3 | 直角流派应全 0 |
| **I6** | `--measure` 存在 | 全站统一容器宽 | 禁止各区块自定 `max-width` |
| **I7** | 字距方向 | 大字负、微标签正 | 唯一例外：东方留白/书法流派的疏朗大字可微正（≤0.04em），须有意为之 |

### S 级

| 编号 | 判据 | 说明 |
|---|---|---|
| **S1** | 阴影策略 | 零装饰流派用 `--line` 分区替代阴影 |
| **S2** | 缓动曲线 | 除非零动效，`linear` 会显得机械 |
| **S3** | 分割手段存在 | 既无线也无边框时，确认分区靠什么 |
| **S4** | 等宽/表格数字 | 价格、规格、时间列需 `tabular-nums` |
| **S5** | `never` 清单存在 | MASTER.md 应显式写出禁止项，且可被检查 |

---

## 规范 token 词汇表（N1）

用同义词不影响使用，但会拖慢跨项目复用与交接。**建议**用下列规范名：

| 角色 | 规范名 | 常见同义词（脚本认，但建议收敛） |
|---|---|---|
| 页面底色 | `--bg` | `paper`、`background`、`surface`、`canvas` |
| 次级底 | `--bg-soft` | `paper-2`、`surface-2`、`bg-alt` |
| 正文色 | `--fg` | `ink`、`text`、`foreground` |
| 次要文字 | `--muted` | `ink-60`、`text-muted`、`gray` |
| 分割线 | `--line` | `ink-12`、`rule`、`border`、`hairline` |
| 强调色 | `--accent` | — |
| 可作正文链接的强调色 | `--accent-ink` | `accent-text`、`link` |
| 强调色淡底 | `--accent-soft` | — |
| 标题字体 | `--font-display` | `font-heading`、`serif`（旧命名） |
| 正文字体 | `--font-body` | `font-sans`、`sans`（旧命名） |
| 等宽字体 | `--font-mono` | `font-code` |
| 中文回退 | `--font-cjk` | `font-chinese` |
| 字号 | `--fs-{xs,base,lg,xl,2xl,display}` | `fs-body`/`fs-sm`/`fs-mega` 等（脚本按 `fs-` 前缀收集） |
| 行高 | `--lh-{…}` | `leading-*` |
| 间距 | `--space-1…8` | `sp-1…8` |
| 字距 | `--trk-{display,heading,body,label}` | `tracking-*`、`ls-*` |
| 容器宽 | `--measure` | `container`、`max-width`、`wrap` |
| 正文列宽 | `--prose-width` | `measure-text`、`line-length` |
| 页面边距 | `--gutter` | — |
| 圆角 | `--radius` / `--radius-sm` / `--radius-lg` | — |
| 动效 | `--dur-fast` / `--dur-normal` / `--ease` | `duration-*`、`ease-out` |
| 中轴 | `--axis` | 无（只在 MASTER.md 登记） |
| 底色档 | `--surface` | 无（取值 `white`/`paper`/`tint`/`stone`/`slate`/`ink`/`deep`） |
| 尺度变体轴 | `--variant` | 无（取值 `a`/`b`/`c`/`d`） |

**两种写法脚本都认**：CSS 块 `--x: v;` 与 markdown 表格 `` | `--x` | `v` | ``。
但**唯一真源应是一份 CSS 文件**（如 `src/styles/tokens.css`），MASTER.md 是它的可读副本
—— 两份值不一致就是漂移（`--anchor` 校验会暴露）。

---

## 脚本查不了的：六条人眼判据

- [ ] 缩到 375px，**标题没有一行只剩 1–2 个汉字**（孤字）——有就降一档字号或改断行
- [ ] 最长那段正文，**回行时不跳行**
- [ ] 中英混排处**没有粘连**（英文单词贴着汉字）
- [ ] 关掉全部动效后，**阅读节奏依然成立**（靠留白而非动画）
- [ ] 点开任意一个详情页：**它有没有索引页看不到的信息**（元信息 / 完整图组 / 更长的说明）？
      只是把封面图放大 = 假子页，拆了等于没拆
- [ ] 页内跳转点下去：**落点是那一节的标题**（不是差半屏），且键盘焦点真的跟过去了
      （F15 只查「锚点存不存在」，落点准不准只能靠眼睛）

---

## 一个真实案例（为什么这份文件值得存在）

一份作品集站曾被本脚本判为 **F 2**：

1. `--text-muted` 合成后仅 **2.63:1**（图注、灯箱计数都用它）—— a11y 不达标
2. 字阶 **7 级**，其中 `--fs-1` 实际 **0 次使用**（死 token）

修法：把 `0.62` 那一档下移给 `muted`、补 `0.72` 给 `secondary`（三档变成 16.7 / 6.7 / 4.8:1），
删掉死 token `--fs-1`。同时发现 `MASTER.md` 与真正生效的 `tokens.css` **色值不一致**
（`#FFFFFF`/`#B83232` vs 实际 `#FAFAF8`/`#B33A2A`）—— 这类「文档与实现漂移」只有
逐值比对才会暴露。
