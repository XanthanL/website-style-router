# Layouts — 布局原型（visual skeleton 五选一）

风格锚点声明它绑定哪个布局原型。输出时**套该原型的骨架**，而不是共享模板换色——这是 v1「排版雷同」的修法。

每个原型给：结构、何时用（对应风格）、骨架片段（Astro/HTML 语义骨架，token 来自 MASTER.md）。

---

## 1 · utility-board（信息扫读板）

**结构**：顶栏（品牌 + 唯一状态/今日标识）→ 主区为高密度清单或表格（等宽数字、状态标签非仅靠颜色）→ 极简页脚。无 hero 大图、无氛围区块。

**何时用**：状态频繁变动、回头客来查"今天有什么/还有没有"、视觉资产低或禁图、需快速扫读。
**绑定风格**：`swiss-utility`、`data-dense-app`、`brutalist-raw`（minimal-state 变体）。

**骨架**：
```
<header class="board-bar"> 品牌 · 今日状态 </header>
<main>
  <table class="ledger">  ← 行=条目，列含 状态(文字标签)/规格(等宽)/操作
    <tbody> ... </tbody>
  </table>
</main>
<footer> 维护提示（改一个数据源文件即更新） </footer>
```

---

## 2 · editorial-hero（编辑式大标题）

**结构**：全宽 hero（超大标题 + 一句主张 + 主 CTA ± 次 CTA）→ 2–4 个可扫读区块（图文交错，图非必需但 hero 后可承载）→ 信任/合规条 → 页脚。

**何时用**：有强主张/故事、需要让人"读完"或"看完下单"、转化或叙事导向、有图或允许氛围图。
**绑定风格**：`provisions-label`、`warm-hospitality`、`soft-organic`、`retro-nostalgic`。

**骨架**：
```
<header class="hero"> 超大标题 · 主张 · [主CTA] [次CTA] </header>
<section class="block"> 图文交错区块 ×N </section>
<section class="trust"> 合规/信任信息（不可折叠） </section>
<footer> </footer>
```

---

## 3 · catalog-grid（目录网格）

**结构**：分类/筛选条 → 卡片网格（图 + 名 + 价/年份 + 标签）→ 详情展开。密度中等，靠缩略图扫读。

**何时用**：条目多（15+）且用户来"挑/比/扫"——SKU 目录、作品集、档案索引都算。这条同时覆盖两个变体：
- **商业变体**：SKU / 价签 / 筛选（`corporate-structured`、`glass-tech`）
- **作品变体**：作品卡（图 + 标题/年份/媒介）、无价签、无筛选，中轴常为 `center-axis`（`diagonal`、`archival-index`、`industrial-documentary`）

**绑定风格**：`corporate-structured`、`glass-tech`、`diagonal`、`archival-index`、`industrial-documentary`。

**骨架**：
```
<nav class="filters"> 分类/筛选（作品变体可省） </nav>
<main class="grid"> <article class="card"> 图·名·价或年份·标签 </article> ×N </main>
```

> 反例记档：真实作品集（17 件作品）选的是**本原型而非 `split-narrative`** —— 逐一长滚不适合多件作品；
> `split-narrative` 退为点开单件后的详情/灯箱阅读。见 `examples/vivian-peng-portfolio/content-profile.md`。

---

## 4 · split-narrative（非对称分栏叙事）

**结构**：左文右图或交错分栏，大留白，字体即图形，节奏慢。适合长文或作品即界面。

**何时用**：作品集/艺术家、图文并重、留白多、受众有耐心。
**绑定风格**：`editorial-print`、`quiet-luxury`、`expressive-artistic`。

**骨架**：
```
<main class="split">
  <div class="col-text"> 标题 · 长文/主张 </div>
  <div class="col-media"> 大图/作品 </div>
</main>
（可多组交错）
```

---

## 5 · minimal-state（单屏单动作）

**结构**：一屏，一个主动作，其余收起。无导航、无多区块。

**何时用**：一个动作（预约/下单/登记/留资），无干扰，内容极少。
**绑定风格**：`agentic-minimal`、`brutalist-raw`、`glass-tech`。

**骨架**：
```
<main class="single"> 一句话 · [唯一主动作] </main>
```

---

## 中轴策略（axis）— 必须显式写进 token

「页面感觉统一」的实际来源往往不是配色，而是**中轴的一致性**。
本次真实教训：肖像、封面、正文的中轴被用户逐个指出（"可以居中吗""稍微放大一点"）——**一次定清楚，别等反馈**。

三档，选 1，落成 MASTER.md 里的 `--axis`：

| 值 | 含义 | 典型出现 |
|---|---|---|
| `left-rail` | 所有区块左对齐到同一基线 | 信息型 / 台账型 / 单列长文 |
| `center-axis` | hero / 肖像 / 封面 / 宣言居中；**正文保持左对齐** | 人物页、作品集、宣言型 |
| `split` | 左文右图，中轴由栏宽比例定义 | 长文、作品即界面 |

> **优先级：锚点已声明的 `axis` 为准**（`styles/index.json` 的 40 个锚点每个都有 `axis` 字段）；
> 本表用于**理解与兜底**，不是「布局原型 → 中轴」的硬映射。
> 已知反例（说明为何不能硬映射）：`editorial-hero` 既可能是 `center-axis`（宣言型 hero 居中）
> 也可能是 `left-rail`（转化型 hero 左对齐）——真实按 `provisions-label` 做的转化页用的是 `left-rail`，
> 与 `styles/index.json` 的声明一致。别用原型名反推中轴。
> 反向反例同样存在：`luxury-retail` 是 `catalog-grid` 原型，但 `axis: split`
> （单件大图 + 极简信息 = 左文右图）。**两个方向都不能硬映射。**

**供给分布**（`styles/index.json`，2026-09 实测）：`left-rail` 19 · `center-axis` 16 · `split` 5。
`split` 跨 2 个布局原型、4 个族 —— 这是刻意的：某一档中轴如果只有两三个锚点、
还全挤在同一个布局原型里，它在批次里就不是「一档选择」而是**必经点**
（要凑够中轴种类就得踩它，踩它就必然带出同一个布局），布局与族的自由度会被中轴一起锁死。
`scripts/validate_skill.py` 的 **H3** 用「每档 ≥3 锚点 / ≥2 布局 / ≥3 族」钉住这件事。

**批次里的中轴判据是「不塌缩」，不是「凑齐档位」**：`ledger --report` 要求中轴 ≥2 种
**且单档占比 ≤60%**。不要写成「3 种全出现」——中轴总共就 3 档，要求全出现等于覆盖率 100%，
那是配额不是多样性，会强制每个批次都含一个 `split` 站。

规则：

1. `--measure` 定义主容器宽度，所有对齐都用它，**不要每个区块各写一个 max-width**。
2. **图与图注必须同轴。** 图片居中了、figcaption 还左对齐，是最常见的漏改。
3. **居中 ≠ 全部居中。** 正文、列表、表格保持左对齐（中文超过 3 行居中极难读）——写进 `never` 清单。
4. 图片尺寸调一次要连带检查：同组内其他图是否失衡、移动端断点是否同步改。
5. **中轴必须真的写进 MASTER.md**（`--axis` 是 G3 门的出口检查项）。三个样例的实测值可对照：
   `swiss-utility` → `left-rail`、`provisions-label` → `left-rail`、`diagonal` → `center-axis`。

---

## 页内导航（按布局原型）

**档位由内容决定**（`architectures/granularity.md`），但**形态由布局原型决定** ——
同样是 `anchor-jump`，台账页贴一行文字链接就够，编辑式长页要的是带编号的行内目录。
**贴一个和设计系统无关的浮动目录组件，是本节能防住的主要错误。**

| 原型 | 该原型的页内导航形态 | 别做什么 |
|---|---|---|
| `utility-board` | 顶栏下方一行锚点（`跳到：豆单 / 营业信息 / 规则`），小号、等宽、低调；或左侧 label rail 兼作导航 | 不要浮动侧栏目录 —— 台账页的信息密度已经很高 |
| `editorial-hero` | 行内目录（章节编号 + 标题，排在 hero 之后）；或 sticky 顶栏目录 | 纯转化页不要放整页目录（见 `conversion.md`） |
| `catalog-grid` | 分类筛选条**兼作**导航（分类即锚点）；作品变体可用年份 / 媒介做锚点分组 | 不要给网格页加章节目录 —— 网格本身就是索引 |
| `split-narrative` | 左栏固定目录（文随目录滚动），或跨栏横线做章节断点并配锚点 | 不要全屏浮层目录 —— 破坏分栏节奏 |
| `minimal-state` | `none` —— 单屏没有可跳的目标 | 任何目录都是干扰 |

规则：

1. **锚点 id 用语义名**（`#spec` / `#faq` / `#compliance`），不要用 `#section-3` —— 顺序一变就错。
2. **目录自身也是设计对象**：字号取 `--fs-xs`、字距取 `--trk-label`、颜色取 `--muted`，
   位置由 `--axis` 决定。它属于这套设计系统，不是外挂组件。
3. **移动端降一档**：`sticky-toc` 在窄屏折叠成「目录」按钮，展开为浮层；
   不得常驻挤占首屏超过 1/3 高度。
4. **跳转后焦点落在目标**：目标区块加 `tabindex="-1"` 并在点击后 `focus()`，
   否则键盘用户跳完仍停在原地。
5. **导航文案与区块标题一致**：目录写「配料表」，区块标题也得是「配料表」——
   不一致会让人怀疑跳错了地方。
6. **静态锚点用普通引号写**：`href="#faq"`，不要写成模板字符串 ——
   判据（F15）只认静态锚点，模板串会被当成动态值跳过，等于没做校验。

---

## 多图条目与图库（组件级硬规则）

**字段定义了就必须渲染出来。** 本次真实事故：数据里每个作品有 `images: [...]`，
页面却只渲染 `images[0]` —— **49/62 张图根本没出现在站上，而"引用完整"的校验还是 PASS**，
因为引用确实都写进数据了。**校验要看渲染结果，不能只看数据。**

规则：

1. 条目含多图 → 必须提供图库视图，且**每张图可达**（不是只给第一张当缩略图）。
2. 图库最小配置：缩略图条（可点）+ `当前/总数` 计数 + 前后翻页 + 键盘左右 + Esc 关闭。
3. 同一图库内保持单一展示尺度（容器高与 `object-fit` 一致），禁止 contain/cover 混用导致缩放跳动。
4. 图注 / 标题 / 媒介字段随当前图同步更新（双语站要一起切）。
5. 交付前双重核对：
   ```bash
   python scripts/verify_assets.py <assets_dir> <data.json>   # 数据侧：缺失/未用/格式
   curl -s <url> | grep -oE 'src="[^"]*pdf-img[^"]*"' | sort -u | wc -l   # 渲染侧：实际出现的图数
   ```

---

## 视觉签名（反同质化）★交付前必填

**布局原型只决定骨架，不决定「像不像同一个模板」。** 骨架相同、token 不同，
观感仍可能雷同 —— 这就是「只是不同的 Markdown 组织器」的成因。修法是要求每个产出
声明 **≥3 条结构级视觉签名**：不是颜色、不是圆角，而是**能在渲染结果里指出来的结构手法**。

### 签名菜单（按布局原型给候选，选 ≥3 条并实际做出来）

| 原型 | 可选的视觉签名（每条都要能在渲染结果里指出来） |
|---|---|
| `utility-board` | ① 通栏细线网格（可见的列分隔线）② 等宽数字右对齐成列 ③ 状态文字标签（非颜色）④ 顶部 LIVE / 今日戳 ⑤ 零卡片、零阴影 ⑥ 固定行高的台账行（行高锁死，扫读节奏一致）⑦ 左侧固定标签栏（label rail，字段名独占一窄列）⑧ 大写等宽微标签 + 正字距的 KICKER 行 |
| `editorial-hero` | ① 超大标题压过首屏 ② 首字下沉 / 悬挂标点 ③ 单一主 CTA + 次要文字链接 ④ 图文交错（左右交替）⑤ 大标题负字距收紧 ⑥ 正文分栏（2–3 栏）⑦ 引用块跨栏出血（pull quote 撑到版心外）⑧ 章节编号用超大字号当装饰（01 / 02） |
| `catalog-grid` | ① 非对称栏比（如 2:1 / 3:2）② 图片按原比例（不裁统一方格）③ 悬停显元数据 ④ 编号 / 年份当视觉锚 ⑤ 空槽位用留白而非占位图 ⑥ 网格基线可见（细线分隔行列）⑦ 主图跨格（feature 项占 2×2）⑧ 标签贴在图片角上（旋转或色块） |
| `split-narrative` | ① 左文右图固定栏比 ② 大留白（区块间距 ≥ 2 倍常规）③ 字体即图形（大字标）④ 图与图注同轴 ⑤ 滚动中的节奏停顿 ⑥ 粘性侧栏（一侧滚动时另一侧固定）⑦ 交错栏（奇偶区块左右互换）⑧ 跨栏横线做章节断点 |
| `minimal-state` | ① 单屏单动作 ② 大量负空间 ③ 唯一强调色只用在动作上 ④ 无导航 ⑤ 排版承担全部视觉重量 ⑥ 视口单位锁定（`100dvh`，不出现滚动条）⑦ 输入控件本身即视觉中心（无边框，只有底线）⑧ 状态切换用原地替换而非跳转 |

### 组合规则

1. **同一批交付里，任意两个站的签名组合不得完全相同。** 3 条签名从 8 个里选，
   组合空间足够（C(8,3) = 56）；撞了就是没做。
2. **签名必须能被指出来**：在 `content-profile.md` 里逐条写明「在渲染结果的哪个位置能看见」。
   写「高级感」不算签名，写「价格列等宽右对齐，见第 3 屏表格第 4 列」才算。
3. **签名要过「关掉颜色还成立吗」这一问**：把页面转成灰度截图，签名仍在 → 是真结构签名；
   消失 → 那只是配色，不是签名。
4. 签名与 `never` 清单冲突时以 `never` 为准（例如 `swiss-utility` 禁用居中标题，
   就不能把「居中大标题」当签名）。
5. **同一锚点第二次使用必须换组。** 若 `scripts/ledger.py --check` 显示该锚点已在本批次用过，
   签名组合**至少换 2 条**，并同时换 `--variant` 与 `--surface`。**原样复用 = 同一套设计交付两遍。**
6. **签名不写在 MASTER.md 里 = 没做。** `audit_tokens.py` 的 **F10** 会检查该段存在且 ≥3 条；
   缺失是 F 级失败，不是建议。

---

## 选型时的布局判定顺序

1. 风格锚点已声明 `layout` → 直接采用其绑定原型（除非约束明确否决，见 intake 排除布局列）。
2. 若风格未绑定或约束冲突 → 按上表「何时用」反查：无图+高频 → utility-board；有图+转化 → editorial-hero；SKU 多 → catalog-grid；图文并重 → split-narrative；单动作 → minimal-state。
3. **同一 IA 可套不同布局**：conversion IA 在「单品强主张」时走 editorial-hero，在「多 SKU 挑选」时走 catalog-grid——这就是不同产品得到不同版面的机制。
