# 交付前验收

逐条打勾，任一条不过不许交付。**门（G）编号对应 SKILL.md 的阶段门表。**

## 阶段门对账（G-1 … G6）
- [ ] **G-1** 三零成立（缺失 0 / 未用 0 / 格式 0），体积 < 15MB
- [ ] **G0** `decide.md` 的 **C 类问题全部有答案**（源 / 形态 / 转化目标 / 参考站 / 语言 / 实体信息 / 维护者 / 合规红线 / 部署与 SSR）
- [ ] **G1** 排除清单 ≥3 项且写明被谁排除
- [ ] **G1.5** `tech-stack.md` 存在：D1–D8 判定 + 选定栈 + 被排除项（含被哪条判据排除）+ 3 条可观测证伪条件 + **头部写明 skill 版本号**
- [ ] **G1.6** `ledger.py --check` **无高冲突**（锚点 / display 字体必须唯一；底色档 / 中轴在同批次 ≤2 次）
- [ ] **G1.7** `content-profile.md` 含「页面粒度与页内导航」段：**两轴都有结论且都有理由**（不是默认值）；粒度被推到 `master-detail`/`multi-page` 时已把工作量级别改为 L2
- [ ] **G2** 用户**明确点头**过（技术栈 + IA + **页面粒度 + 页内导航** + 锚点 + layout + axis + **底色档 + 变体轴 + 字体对 + ≥3 条视觉签名** + 语言），且写了 3 条证伪条件
- [ ] **G3** `MASTER.md` 含 `--axis`、**`--surface` / `--variant`**、**字体搭配四变量**、**视觉签名段（≥3 条）**；`embedded` 另出 `HOST.md`（含宿主令牌来源文件路径）
- [ ] **G4** 按 `tech-stack.md` 选定栈构建通过（`plain-html` 直接开；`astro`/`next`/`vite` 用 `npm run build`；宿主下 `tsc --noEmit`），且**页面文件数与声明的粒度一致**
- [ ] **G5** 本文件全部通过 + **`audit_tokens.py --site <站点目录>` 通过（F14/F15）** + **`audit_tokens.py --batch` 批次差异度通过**，或未过项标 `[未定]` 并说明
- [ ] **G6** 交付说明里交代了三件事：我自主决定了什么 / 哪些是推断的（依据）/ 哪些还没确定

## 技术栈选型合规（G1.5）
- [ ] `tech-stack.md` 里 D1–D8 每一项都有取值**和依据**（来自哪条 Grill 答案 / 哪份源材料）
- [ ] 技术栈是**排除 + 定序**得出的，不是「默认继承 Astro」或「挑一个顺手的技术」
- [ ] 被排除项逐条写明**被哪条判据排除**（不是笼统的「不合适」）
- [ ] 有 **3 条可观测的证伪条件**（能拿具体数字或具体事件检验，不是「如果不好用」）
- [ ] 产物的**目录形状与选定栈一致**（`plain-html` 不该有 `src/pages/`；`astro` 不该是单文件 HTML）
- [ ] 字体加载策略与栈匹配（`fonts.json` 的 `loading` 字段：自托管 / CDN / 系统）
- [ ] `embedded` 时服从宿主栈，未引入第二套构建链

## 批次差异合规（G1.6 · v0.8 新增，跨站去重）

> **单站自洽 ≠ 批次多样。** 实测 5 个测试站逐站跑审计都能过，摆在一起却是
> `--bg` 5/5 全 `#FFFFFF`、40 个锚点只用了 3 个、视觉签名 5/5 缺失。
> 这一节补的是「跨站差异」这条腿。

- [ ] 开工前跑过 `python scripts/ledger.py --check`，且**无高冲突**
- [ ] **锚点**在本批次内唯一（`ledger.py` 的高冲突项为空）
- [ ] **display 字体**在本批次内唯一（同上）
- [ ] **底色档** `--surface` 在本批次内出现 ≤2 次（是**显式选的**，不是默认滑入的白）
- [ ] **中轴**在本批次内出现 ≤2 次
- [ ] 若该锚点本批次已用过 → 已换 `--variant` × `--pair` × `--surface` 组合，**不是原样复用**
- [ ] 交付前跑过 `python scripts/audit_tokens.py --batch <批次目录>`，**F11 通过**
- [ ] 交付后跑过 `python scripts/ledger.py --commit` 记账（不记账，下一个站查不了重）
- [ ] 记账时带了 `--font-display <首族名>`（漏记 → 批次报告判「未记录」不通过）
- [ ] `ledger.py --report` 的 6 项全 ✓（锚点唯一率 ≥80% / 底色 ≥3 种 / **中轴 ≥2 种且单档占比 ≤60%** / display 唯一率 100% / 族覆盖 ≥60%）
  - 中轴**不是**「3 种全出现」：中轴总共就 3 档，要求全出现是配额不是多样性，会强制每批都含一个 `split` 站

## 页面粒度与可导航性（G1.7 · v0.9 新增，功能完整性）

> **设计达标 ≠ 功能达标。** 实测的失败模式：页面好看，但只有单个 `index`
> （17 件作品全塞一页、单件无法被分享），长页也只能一翻到底。
> 审美判据全过，浏览体验照样不合格。这一节补的是「内容能不能被找到」这条腿。

- [ ] `content-profile.md` 有「页面粒度与页内导航」段，**两行都填了**
- [ ] **粒度结论不是默认值**：能追到 `entries` / `depth` / `shareable` / `indexable` / `themes` 中的具体几条
- [ ] 跑过 `python scripts/pick_pages.py`，且**被排除的档位**逐条写明了理由
- [ ] 若判为 `single`：写明**为什么不需要拆页**（条目少 / 浅 / 不可分享），不是「没想过」
- [ ] 若判为 `master-detail` / `multi-page`：**页面清单列得出来**，且详情页有索引页没有的信息（不是把封面放大）
- [ ] 若判为 `master-detail` / `multi-page`：**数据仍只有一份真源**（详情页从同一份数据生成，不是抄两份）
- [ ] 若维护者非技术且不能静态生成 → 已按否决条件降级，且**在交付说明里写明了代价**
- [ ] 页内导航档位能追到 `blocks` / `lookup` / `linear` / `screens`
- [ ] 声明了导航的页面：**每个 `href="#x"` 都有对应的 `id="x"`**（无断链）
- [ ] 声明了导航的页面：跳转后焦点落在目标区块（`tabindex="-1"` + `focus()`）
- [ ] 声明 `none` 的页面：区块数确实不多（**长页声明无导航 = 用户只能一翻到底**）
- [ ] 移动端目录不常驻挤占首屏超过 1/3 高度
- [ ] `python scripts/audit_tokens.py --site <站点目录>` **F 级 0**

## 源与形态（G-1）
- [ ] `source-map.md` 存在，含「源 / 内容映射 / 未采用 / **提取告警**」四段
- [ ] 每种输入源都被识别并走了对应处置路径（见 `source.md` 的 13 类表）
- [ ] `_WARNINGS.txt` 里**每条告警都已在 source-map 记明影响与处置**（没处置不许交付）
- [ ] 本机无法处理的源（扫描件 / 旧版 Office / 视频旁白）**已明确告知用户**，未假装能处理
- [ ] 交付形态已明确（`standalone` / `embedded` / `system-only`），且与该形态的附加要求一致
- [ ] 有参考站时 `references.md` 存在，且写了「**不学它的什么**」（≥2 条）
- [ ] 素材体积 < 15MB，或已做压缩/响应式化

## 决策权合规（G0）
- [ ] 没有任何 **C 类问题**被自行猜测填值
- [ ] 每个 B 类决定都写了推荐答案 + 依据，不是直接抛出问题
- [ ] 提问轮次 ≤3 轮（超过说明源材料不足，而非该继续问）
- [ ] 没有把 A 类问题（文件名、间距数值等）拿去问用户
- [ ] 触及 **D 类红线**时已警示用户而非照做

## 风格收敛合规（G1→G2）
- [ ] 走的是**两级收敛**（族 → 锚点），不是直接从 40 个里挑
- [ ] 选族的依据是「内容支撑力 / 转化目标 / 密度诉求」三项，**不是品类**
- [ ] 只读了命中的那 1 个 spec，没有把 `styles/specs/` 全量读入
- [ ] 命中 `warm-hospitality` 时，两条前置条件（专业摄影 + 卖空间体验）**均已验证成立**

## 回源可追溯（抄错率最高的四类：年份 / 作品名 / 人名 / 数字）
- [ ] 每个字段都能指回源（`p12 · "原句"` 或 `<url> #selector`）
- [ ] **年份、作品名、人名、数字四处逐条回源抽查过**
- [ ] 指不回源的字段不在页面上，而是进了「待补」表
- [ ] 「待补」表里的每一项都在页面上有可见标记（`[待填]` + 红色虚线框）

## 选型可追溯
- [ ] `content-profile.md` 里每条风格/IA/布局结论都能追到 intake 的某条约束
- [ ] "取 X 因为…" 那句是**可证伪的具体理由**，不是"更合适""更贴合"
- [ ] 排除清单里至少列了 3 项，且写清被谁排除的
- [ ] `--axis` 中轴策略已确定并写进 MASTER.md

## Token 可执行
- [ ] `design-system/MASTER.md` 全部是 CSS custom properties，零形容词
- [ ] 有显式 `never:` 清单，且清单项可被机器/人检查（如"居中正文"可查，"有质感"不可查）
- [ ] 字阶不超过 6 级，间距栅格不超过 8 级
- [ ] `embedded` 形态：`design-system/HOST.md` 存在，且记录了宿主令牌的来源文件路径

## 内容真实性
- [ ] 页面上的每个字都来自已有内容或用户确认过的文案，**没有一个是 agent 编的**（假地址、假评价、假年份一律不可接受）
- [ ] 图片位若暂无素材，是**明确的占位结构**而非假图

## 渲染完整性（数据对 ≠ 渲染对）
- [ ] 多图条目：每张图**都能在渲染结果里可达**（不是只渲染第一张）
- [ ] 图库有完整交互：缩略条 / `当前/总数` 计数 / 前后翻页 / 键盘左右 / Esc
- [ ] 抓渲染后的 HTML 数一遍 img 数量，与数据里的引用数一致：
      `curl -s <url> | grep -oE 'src="[^"]*(pdf-img|images)[^"]*"' | sort -u | wc -l`
- [ ] 同一图库内展示尺度统一（无 contain/cover 混用造成的跳动）

## 语言（双语站）
- [ ] 所有可见文案都挂上了双语字段（**包括灯箱、按钮、图注、空状态**）
- [ ] `<html lang>` 与 `<title>` 随语言切换
- [ ] 语言偏好持久化（localStorage），且 `<head>` 前置脚本**防首屏语言闪烁**
- [ ] `embedded` 时译文**复用宿主档案的既有译文**，不另写一份
- [ ] 图片 `alt` 也随语言切换

## 中轴与尺寸
- [ ] 所有区块共用同一个 `--measure`，无各自为政的 max-width
- [ ] **图与图注同轴**（图片居中了、图注还左对齐 = 常见漏改）
- [ ] 正文/列表/表格保持左对齐（即使在 center-axis 站上）
- [ ] 主图 / 肖像 / 封面三档宽度符合落定时确认的值
- [ ] 移动端断点同步调整过（不只改桌面端）

## 站点外壳（`embedded` 必查）
- [ ] 顶栏与宿主同构：字标、栏目组与顺序、fixed、滚动态、当前页高亮、移动端抽屉
- [ ] 底栏与宿主同构：字标、标语、版权年份、地点行、colophon 署名
- [ ] 色板 / 字体栈 / 圆角 / 动效曲线取自宿主，未引入第二套
- [ ] **宿主其他实体路径回归正常**（本次是另一位艺术家仍走旧渲染路径）
- [ ] 链接指向宿主真实地址（非 `#`）

## 可运行
- [ ] 按 `tech-stack.md` 选定栈验证通过（`plain-html`：浏览器直接打开无报错；`astro`/`next`/`vite`：`npm run build` 零报错；宿主项目下 `npx tsc --noEmit`）
- [ ] 移动端 375px 宽无横向滚动
- [ ] 无控制台报错
- [ ] 宿主因环境原因起不了 dev 时，**明确告知用户并给出自行验证的命令**，不假装验证过

## 素材校验（从 PDF / 设计稿取图时必做）
- [ ] **逐个用 PIL 读 `Image.format`，确认与扩展名一致**——不要只看 HTTP 200
  - pypdf/PyMuPDF 按 PDF 的 `/Filter` 猜扩展名，`JPXDecode` 流会被写成 `.png`，
    内容是 JPEG2000。Chrome/Edge 解不了 → 状态码 200、无报错、页面只留空框。
  - 一键跑：`python scripts/verify_assets.py <assets_dir> <data.json> […更多数据文件]`
- [ ] 引用集合与磁盘文件名集合双向比对：缺失 = 0、未使用 = 0
- [ ] 图片总体积 < 15MB（原图常 3–5 倍于此）

## 可访问性底线
- [ ] 正文对比度 ≥ 4.5:1
- [ ] 所有可点元素有 focus 态
- [ ] 图片有 alt（装饰图用空 alt）
- [ ] 状态不靠颜色单独传达

## 合规与占位（卖实物商品时）
- [ ] 品名/配料/净含量/保质期/贮存/生产者/SC 编号全部在页面上，未折叠、未隐藏、字号 ≥12px
- [ ] 无疾病预防或治疗功能宣称
- [ ] 无限极词（最 / 第一 / 国家级 / 顶级）
- [ ] 无编造的评价、销量、排名
- [ ] 所有缺失数据以 `[待填]` 呈现且**在页面上有可见标记**（不是静悄悄的占位文案）
- [ ] `content-profile.md` 末尾有「假设与待确认」表，逐项列出来源

## 反 AI 味
- [ ] 全站强调色 ≤ 2 个
- [ ] 没有 `transition: all`
- [ ] 没有无意义的渐变、发光、模糊
- [ ] 没有 emoji 当图标
- [ ] 首屏不出现三等分卡片阵（"我们的优势 1/2/3"）
- [ ] 关掉所有动效后，页面依然完整可用

## 反同质化（v0.7 · 交付前必过）
> 只查数值与流程，不查**差异**，就会得到一批数值全过、观感全同的站 ——
> 「千篇一律、只是不同的 Markdown 组织器」就是这么来的。以下每条都要能拿证据。
- [ ] **≥3 条结构级视觉签名**已写进 `MASTER.md` 的「视觉签名」段，且每条都能指到渲染结果的具体位置（`audit_tokens.py` **F10** 机器校验）
- [ ] 签名过「灰度测试」：转成灰度截图后签名仍在（消失的只是配色，不是签名）
- [ ] 签名不与锚点的 `never` 冲突
- [ ] **同一批交付里任意两个站的 `--font-display` 不同**（`audit_tokens.py` F6 / F11 可查）
- [ ] 同一站内标题与正文不同字体
- [ ] **底色档 `--surface` 是显式选的**，且同批次内不超过 2 次同一档
- [ ] 产物的**目录形状反映了技术栈**（不同需求的产物不该长成同一个形状）
- [ ] 把两个产出并排放在一起，**第一眼能看出是两个不同的站**（人眼终审；做不到就回 G1.6/G2 重做选型）
